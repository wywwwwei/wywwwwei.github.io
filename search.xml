<?xml version="1.0" encoding="utf-8"?>
<search>
  <entry>
    <title>C++ Rvalue Reference</title>
    <url>/2020/07/01/rvalue/</url>
    <content><![CDATA[<h1 id="C-Rvalue-Reference"><a href="#C-Rvalue-Reference" class="headerlink" title="C++ Rvalue Reference"></a>C++ Rvalue Reference</h1><h2 id="Value-categories"><a href="#Value-categories" class="headerlink" title="Value categories"></a>Value categories</h2><img src="/2020/07/01/rvalue/value_categories.png" class="" title="Value-categories">

<p>每个 C++ 表达式（带有操作数的操作符、字面量、变量名等）都属于三种基本值类别中的一种：</p>
<p>更具体的示例描述请查看 <a href="https://en.cppreference.com/w/cpp/language/value_category" target="_blank" rel="noopener">Value categories – cppreference.com</a></p>
<ul>
<li><p>纯右值 (prvalue)</p>
<p>纯右值表达式没有可通过程序访问的地址。</p>
<p>纯右值表达式的示例包括：（除了字符串字面量之外的）字面量、返回非引用类型的函数调用，以及在表达式评估期间创建但仅由编译器访问的临时对象</p>
</li>
<li><p>亡值 (xvalue) </p>
<p>亡值表达式的地址不能再由您的程序访问，但可用于初始化 rvalue 引用，后者提供对表达式的访问。</p>
<p>亡值表达式的示例包括返回右值引用的函数调用，以及数组下标、成员和指针到数组或对象为右值引用的成员表达式</p>
</li>
<li><p>左值 (lvalue)</p>
<p>左值具有程序可以访问的地址。</p>
<p>左值表达式的示例包括变量名称，包括<strong>常量</strong>变量、数组元素、返回 lvalue 引用、位字段、联合和类成员的函数调用</p>
</li>
</ul>
<p>随着移动语义引入到 C++11 之中，值类别被重新进行了定义，以区别表达式的两种独立的性质：</p>
<ul>
<li><em>拥有身份 ( has identity)</em>：可以确定表达式是否与另一表达式指代同一实体，例如通过比较它们所标识的对象或函数的（直接或间接获得的）地址；</li>
<li><em>可被移动</em>：移动构造函数、移动赋值运算符或实现了移动语义的其他函数重载能够绑定于这个表达式。</li>
</ul>
<p>C++11 中：</p>
<ul>
<li>拥有身份且不可被移动的表达式被称作<em>左值 (lvalue)</em>表达式；</li>
<li>拥有身份且可被移动的表达式被称作<em>亡值 (xvalue)</em>表达式；</li>
<li>不拥有身份且可被移动的表达式被称作<em>纯右值 (prvalue)</em>表达式；</li>
<li>不拥有身份且不可被移动的表达式无法使用。</li>
</ul>
<p>拥有身份的表达式被称作“泛左值 (glvalue) 表达式”。左值和亡值都是泛左值表达式。</p>
<p>可被移动的表达式被称作“右值 (rvalue) 表达式”。纯右值和亡值都是右值表达式。</p>
<img src="/2020/07/01/rvalue/value-categories.png" class="" title="value-categories">

<h2 id="Lvalue-Reference"><a href="#Lvalue-Reference" class="headerlink" title="Lvalue Reference"></a>Lvalue Reference</h2><p>c++11之前，C ++中仅存在一种引用类型，所以简单称之为<em>引用</em>，而在c++11中其被称为左值引用</p>
<table>
<thead>
<tr>
<th>L-value reference</th>
<th>Can be initialized with</th>
<th>Can modify</th>
</tr>
</thead>
<tbody><tr>
<td>Modifiable l-values</td>
<td>Yes</td>
<td>Yes</td>
</tr>
<tr>
<td>Non-modifiable l-values</td>
<td>No</td>
<td>No</td>
</tr>
<tr>
<td>R-values</td>
<td>No</td>
<td>No</td>
</tr>
</tbody></table>
<p>而对const对象的左值引用可以使用左值或右值进行初始化</p>
<table>
<thead>
<tr>
<th>L-value reference to const</th>
<th>Can be initialized with</th>
<th>Can modify</th>
</tr>
</thead>
<tbody><tr>
<td>Modifiable l-values</td>
<td>Yes</td>
<td>No</td>
</tr>
<tr>
<td>Non-modifiable l-values</td>
<td>Yes</td>
<td>No</td>
</tr>
<tr>
<td>R-values</td>
<td>Yes</td>
<td>No</td>
</tr>
</tbody></table>
<p>对const对象的左值引用非常有用，因为它使得可以将任何类型的参数（左值或右值）传递给函数，而无需复制该参数</p>
<h2 id="Rvalue-Reference"><a href="#Rvalue-Reference" class="headerlink" title="Rvalue Reference"></a>Rvalue Reference</h2><p>右值引用可将左值和右值区分开，可以帮助您不必要的内存分配和复制操作需求，从而提高应用程序的性能。 </p>
<p>相比于左值引用，</p>
<table>
<thead>
<tr>
<th>R-value reference</th>
<th>Can be initialized with</th>
<th>Can modify</th>
</tr>
</thead>
<tbody><tr>
<td>Modifiable l-values</td>
<td>No</td>
<td>No</td>
</tr>
<tr>
<td>Non-modifiable l-values</td>
<td>No</td>
<td>No</td>
</tr>
<tr>
<td>R-values</td>
<td>Yes</td>
<td>Yes</td>
</tr>
</tbody></table>
<table>
<thead>
<tr>
<th>R-value reference to const</th>
<th>Can be initialized with</th>
<th>Can modify</th>
</tr>
</thead>
<tbody><tr>
<td>Modifiable l-values</td>
<td>No</td>
<td>No</td>
</tr>
<tr>
<td>Non-modifiable l-values</td>
<td>No</td>
<td>No</td>
</tr>
<tr>
<td>R-values</td>
<td>Yes</td>
<td>No</td>
</tr>
</tbody></table>
<h2 id="Move-Semantics"><a href="#Move-Semantics" class="headerlink" title="Move Semantics"></a>Move Semantics</h2><p>要实现移动语义，通常向类提供<em>移动构造函数，</em> 并可以选择移动赋值运算符 （<strong>运算符 =</strong>）。 其源是右值的复制和赋值操作随后会自动利用移动语义。 与默认复制构造函数不同，编译器不提供默认移动构造函数。</p>
<p>假设一个类X，其中包含一个/多个指向 创建和拷贝开销较大的对象(资源) 的指针，则无需取消引用该指针并复制其内容，移动构造（拷贝）函数只需复制指针的地址， 这就是移动构造函数带来的性能优势所在（只需要浅拷贝）。</p>
<p>（由于参数是非常量引用，因此它可以修改传递给它的对象，可能具有破坏性）</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line">X&amp; X::<span class="keyword">operator</span>=(<span class="keyword">const</span> X &amp; rhs)</span><br><span class="line">&#123;</span><br><span class="line">    <span class="comment">//make a clone of what rhs's member pointer refers to</span></span><br><span class="line">    <span class="comment">//destruct the resources that this member pointer refers to</span></span><br><span class="line">    <span class="comment">//attach the clone</span></span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">X&amp; X::<span class="keyword">operator</span>=(X &amp;&amp; rhs)</span><br><span class="line">&#123;</span><br><span class="line">    <span class="comment">//swap this-&gt;pointer and rhs.pointer</span></span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">int</span> main()</span><br><span class="line">&#123;</span><br><span class="line">    X a;</span><br><span class="line">    a = X();</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>因此，通过const lvalue 引用或 rvalue 引用参数重载函数，来编写区分不可修改对象 （lvalue） 和可修改临时值 （rvalue） 的代码。</p>
<h3 id="Forcing-Move-Semantics"><a href="#Forcing-Move-Semantics" class="headerlink" title="Forcing Move Semantics"></a>Forcing Move Semantics</h3><p>如果你想通过一个变量(lvalue)来调用移动构造（拷贝）函数，std::move()会将变量(lvalue)转换为右值，使其绑定到移动构造(拷贝)函数</p>
<blockquote>
<p>std::move() does not move, it casts.</p>
<p>std::move()实际上是通过static_cast实现的</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="keyword">return</span> <span class="keyword">static_cast</span>&lt;<span class="keyword">typename</span> <span class="built_in">std</span>::remove_reference&lt;T&gt;::type&amp;&amp;&gt;(t);</span><br></pre></td></tr></table></figure>
</blockquote>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="keyword">template</span>&lt;<span class="class"><span class="keyword">class</span> <span class="title">T</span>&gt; </span></span><br><span class="line"><span class="class"><span class="title">void</span> <span class="title">swap</span>(<span class="title">T</span>&amp; <span class="title">a</span>, <span class="title">T</span>&amp; <span class="title">b</span>) </span></span><br><span class="line"><span class="class">&#123;</span> </span><br><span class="line">  <span class="function">T <span class="title">tmp</span><span class="params">(<span class="built_in">std</span>::<span class="built_in">move</span>(a))</span></span>;</span><br><span class="line">  a = <span class="built_in">std</span>::<span class="built_in">move</span>(b); </span><br><span class="line">  b = <span class="built_in">std</span>::<span class="built_in">move</span>(tmp);</span><br><span class="line">&#125; </span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">main</span><span class="params">()</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    X a, b;</span><br><span class="line">	swap(a, b);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>因为已经将变量强制转换为右值，而接收右值的函数可能会对该变量产生破坏性作用，在此之后使用变量的内容可能会导致不确定的行为，因此在变量经过std::move()之后，需要销毁或重新分配。</p>
<h2 id="Perfect-forwarding"><a href="#Perfect-forwarding" class="headerlink" title="Perfect forwarding"></a>Perfect forwarding</h2><p>​    完美转发可减少对重载函数的需求，并有助于避免转发问题。 当编写一个泛型函数，该函数将<em>引用</em>作为其参数，并将这些参数传递给（或<em>转发</em>）到另一个函数时，可能会出现<em>转发问题</em>。 例如，如果泛型函数采用 <code>const T&amp;</code> 类型的参数，则调用的函数无法修改该参数的值。 如果泛型函数采用 <code>T&amp;</code> 类型的参数，则无法使用右值（如临时对象或非字符串字面量）来调用该函数。</p>
<p>​    通常，若要解决此问题，则必须提供为其每个参数采用 <code>T&amp;</code> 和 <code>const T&amp;</code> 的重载版本的泛型函数。 因此，重载函数的数量将基于参数的数量呈指数方式增加。 利用右值引用，函数模板会推导出其模板自变量类型，然后使用引用折叠规则，函数可接受任意参数并将其转发给另一个函数，就像已直接调用其他函数一样。</p>
<p>引用折叠规则：</p>
<ul>
<li>T&amp;  &amp; -&gt; T&amp;</li>
<li>T&amp;  &amp;&amp; -&gt; T&amp;</li>
<li>T&amp;&amp;  &amp; -&gt; T&amp;</li>
<li>T&amp;&amp;  &amp;&amp; -&gt; T&amp;&amp;</li>
</ul>
<p>使用示例:</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="keyword">template</span>&lt;<span class="keyword">typename</span> T, <span class="keyword">typename</span> Arg&gt; </span><br><span class="line"><span class="function"><span class="built_in">shared_ptr</span>&lt;T&gt; <span class="title">factory</span><span class="params">(Arg&amp;&amp; arg)</span></span></span><br><span class="line"><span class="function"></span>&#123; </span><br><span class="line">  <span class="keyword">return</span> <span class="built_in">shared_ptr</span>&lt;T&gt;(<span class="keyword">new</span> T(<span class="built_in">std</span>::forward&lt;Arg&gt;(arg)));</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>std::forward从概念上所实现：</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="keyword">if</span> (is_lvalue_reference&lt;T&gt;::value)<span class="comment">//Code producing an lvalue reference expects the object to remain valid.</span></span><br><span class="line">  <span class="keyword">return</span> t;</span><br><span class="line"><span class="keyword">return</span> <span class="built_in">std</span>::<span class="built_in">move</span>(t);</span><br></pre></td></tr></table></figure>

<h2 id="References"><a href="#References" class="headerlink" title="References"></a>References</h2><p><a href="https://docs.microsoft.com/en-us/cpp/cpp/rvalue-reference-declarator-amp-amp?view=vs-2019#syntax" target="_blank" rel="noopener">Rvalue Reference Declarator: &amp;&amp; - Microsoft Docs</a></p>
<p><a href="https://docs.microsoft.com/en-us/cpp/cpp/lvalues-and-rvalues-visual-cpp?view=vs-2015" target="_blank" rel="noopener">Value Categories - Microsoft Docs</a></p>
<p><a href="https://en.cppreference.com/w/cpp/language/value_category" target="_blank" rel="noopener">Value categories - cppreference.com</a></p>
<p><a href="https://www.chromium.org/rvalue-references?tmpl=%2Fsystem%2Fapp%2Ftemplates%2Fprint%2F&showPrintDialog=1#TOC-1.-What-makes-rvalues." target="_blank" rel="noopener">Rvalue references in Chromium</a></p>
]]></content>
      <categories>
        <category>Concepts</category>
      </categories>
      <tags>
        <tag>C/C++</tag>
      </tags>
  </entry>
  <entry>
    <title>C++ Smart Pointers</title>
    <url>/2020/08/12/pointer/</url>
    <content><![CDATA[<h1 id="C-Smart-Pointers"><a href="#C-Smart-Pointers" class="headerlink" title="C++ Smart Pointers"></a>C++ Smart Pointers</h1><p>与托管语言不同，C++没有自动垃圾回收功能（在程序运行时释放堆内存和其他资源）。C++程序负责将获取的所有资源返回到操作系统，己动态分配的堆内存由于某种原因程序未释放或无法释放，造成系统内存的浪费被称为<strong>内存泄漏</strong>。在进程退出之前，泄漏的资源对其他程序不可用。总而言之，内存泄漏是 C-style编程中出现 bug 的一个常见原因。</p>
<h3 id="RAII"><a href="#RAII" class="headerlink" title="RAII"></a>RAII</h3><p>现代c++通过在堆栈上声明对象来尽可能避免使用堆内存。当一个资源对于堆栈来说太大时，它应该被一个对象所拥有。当对象被初始化时，它获得它所拥有的资源。然后，该对象负责释放其析构函数中的资源。拥有的对象本身是在堆栈上声明的。对象拥有资源的原则也称为 “<strong>Resource Acquisition Is Initialization</strong>“ 或 <strong>RAII</strong>。</p>
<p>当拥有资源的堆栈对象超出作用域时，将自动调用其析构函数。这样，C ++中的垃圾回收与对象生存期密切相关，并且是确定性的。资源总是在程序的可控的已知位置释放。这利用了核心语言特性(对象生存期、范围退出、初始化顺序和堆栈展开)来消除资源泄漏并保证异常安全性。</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="built_in">std</span>::mutex m;</span><br><span class="line"> </span><br><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">bad</span><span class="params">()</span> </span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    m.lock();                    <span class="comment">// acquire the mutex</span></span><br><span class="line">    f();                         <span class="comment">// if f() throws an exception, the mutex is never released</span></span><br><span class="line">    <span class="keyword">if</span>(!everything_ok()) <span class="keyword">return</span>; <span class="comment">// early return, the mutex is never released</span></span><br><span class="line">    m.unlock();                  <span class="comment">// if bad() reaches this statement, the mutex is released</span></span><br><span class="line">&#125;</span><br><span class="line"> </span><br><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">good</span><span class="params">()</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    <span class="function"><span class="built_in">std</span>::lock_guard&lt;<span class="built_in">std</span>::mutex&gt; <span class="title">lk</span><span class="params">(m)</span></span>; <span class="comment">// RAII class: mutex acquisition is initialization</span></span><br><span class="line">    f();                               <span class="comment">// if f() throws an exception, the mutex is released</span></span><br><span class="line">    <span class="keyword">if</span>(!everything_ok()) <span class="keyword">return</span>;       <span class="comment">// early return, the mutex is released</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>通过使用智能指针进行内存分配，可以消除内存泄漏的可能性。它们的行为类似常规指针，重要的区别是它负责自动释放所指向的对象。<code>shared_ptr</code> 和 <code>unique_ptr</code> 的区别在于管理底层指针的方式：<code>shared_ptr</code> 允许多个指针指向同一个对象；<code>unique_ptr</code> 则“独占”所指向的对象。<code>weak_ptr</code> 是一种弱引用，指向 <code>shared_ptr</code> 所管理的对象。</p>
<p>智能指针是<strong>模板</strong>，当创建时，需要提供指针指向的类型。默认初始化的智能指针保存着一个空指针。</p>
<h3 id="unique-ptr"><a href="#unique-ptr" class="headerlink" title="unique_ptr"></a>unique_ptr</h3><p>只允许底层指针有一个所有者，与原始指针一样高效。<code>unique_ptr</code> 的基本特征：大小是<strong>一个指针</strong>，可移动（将所有权转移到新 <code>unique_ptr</code> 并重置旧 <code>unique_ptr</code>），但<strong>不可复制或共享</strong>。<code>unique_ptr</code> 通过引用来传递，如果尝试通过此处的值传递，由于删除了 <code>unique_ptr</code> 复制构造函数，编译器将引发错误。</p>
<img src="/2020/08/12/pointer/unique_ptr.png" alt="unique_ptr"  />

<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="comment">//Example</span></span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">main</span><span class="params">()</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    <span class="comment">// Use make_unique function.(C++14)</span></span><br><span class="line">    <span class="keyword">auto</span> p1 = make_unique&lt;MyClass&gt;(<span class="string">"argv"</span>);</span><br><span class="line">    <span class="comment">// Move raw pointer from one unique_ptr to another.</span></span><br><span class="line">    <span class="built_in">unique_ptr</span>&lt;MyClass&gt; p2 = <span class="built_in">std</span>::<span class="built_in">move</span>(p1);</span><br><span class="line">    <span class="comment">// Create a unique_ptr to an array of 5 integers.</span></span><br><span class="line">    <span class="keyword">auto</span> p3 = make_unique&lt;<span class="keyword">int</span>[]&gt;(<span class="number">5</span>);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h3 id="shared-ptr"><a href="#shared-ptr" class="headerlink" title="shared_ptr"></a>shared_ptr</h3><p>采用引用计数的智能指针，专门用于可能需要多个所有者管理内存中对象的生命周期的方案。<strong>大小为两个指针</strong>；一个用于<strong>对象</strong>，另一个用于包指向<strong>共享控制块</strong>。</p>
<p>控制块是一个动态分配的对象，其中包含：</p>
<ul>
<li>指向被管理对象的指针或被管理对象本身</li>
<li>删除器（类型擦除）</li>
<li>分配器（类型擦除）</li>
<li>占有被管理对象的 <code>shared_ptr</code> 的数量</li>
<li>涉及被管理对象的 <code>weak_ptr</code> 的数量</li>
</ul>
<p>初始化 <code>shared_ptr</code> 之后，您可以复制它，在函数参数中按值传递它，并将其分配给其他 <code>shared_ptr</code> 实例。 所有实例都指向同一个对象，并且共享对一个控制块的访问权，每当添加新的 <code>shared_ptr</code>，超出范围或对其进行重置时，该控制块都会递增和递减<code>shared_ptr</code> 引用计数。如果<code>shared_ptr</code> 引用计数减至零，控制块就会调用被管理对象的析构函数。但控制块本身直到 <code>weak_ptr</code> 计数器同样归零时才会释放。</p>
<p><code>shared_ptr</code> 的默认内存布局（通过构造函数）</p>
<img src="/2020/08/12/pointer/shared_ptr1.png" alt="shared_ptr" style="zoom: 45%;" />

<p>使用<code>make_shared</code> / <code>allocate_shared</code>创建时的内存布局：</p>
<img src="/2020/08/12/pointer/shared_ptr.png" alt="shared_ptr" style="zoom: 50%;" />

<blockquote>
<p><a href="https://stackoverflow.com/questions/34046070/why-are-two-raw-pointers-to-the-managed-object-needed-in-stdshared-ptr-impleme" target="_blank" rel="noopener">Why are two raw pointers to the managed object needed in std::shared_ptr implementation?</a></p>
</blockquote>
<p>首次创建内存资源时，尽可能使用 <code>make_shared</code> 函数创建 <code>shared_ptr</code>。 <code>make_shared</code> 是 exception-safe 的。它在同一个调用中为控制块和资源分配内存 (<a href="https://stackoverflow.com/questions/20895648/difference-in-make-shared-and-normal-shared-ptr-in-c" target="_blank" rel="noopener">Difference in make_shared and normal shared_ptr in C++</a>)，从而减少了构造开销。 如果不使用 <code>make_shared</code>，则必须先使用显式新表达式创建对象，然后再将其传递给<code>shared_ptr</code>构造函数。 </p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="comment">//Example</span></span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">main</span><span class="params">()</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    <span class="comment">// Use make_shared function when possible.</span></span><br><span class="line">    <span class="keyword">auto</span> sp1 = make_shared&lt;MyClass&gt;(<span class="string">"argv"</span>);</span><br><span class="line">    </span><br><span class="line">    <span class="comment">// Ok, but slightly less efficient. </span></span><br><span class="line">    <span class="function"><span class="built_in">shared_ptr</span>&lt;MyClass&gt; <span class="title">sp2</span><span class="params">(<span class="keyword">new</span> MyClass(<span class="string">"argv"</span>))</span></span>;</span><br><span class="line">	</span><br><span class="line">    <span class="comment">//Initialize with copy constructor. Increments ref count.</span></span><br><span class="line">	<span class="function"><span class="keyword">auto</span> <span class="title">sp3</span><span class="params">(sp2)</span></span>;</span><br><span class="line">    </span><br><span class="line">    <span class="comment">//Initialize via assignment. Increments ref count.</span></span><br><span class="line">	<span class="keyword">auto</span> sp4 = sp2;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h3 id="weak-ptr"><a href="#weak-ptr" class="headerlink" title="weak_ptr"></a>weak_ptr</h3><p>结合 <code>shared_ptr</code> 使用的特例智能指针，不控制所指向对象生命周期的智能指针。<strong>大小为两个指针</strong>；一个用于存储构建它的 <code>shared_ptr</code> 的指针，另一个用于指向 <strong>共享控制块</strong>。将一个 <code>weak_ptr</code> 绑定到一个 <code>shared_ptr</code> 不会改变 <code>shared_ptr</code> 的引用计数，一旦最后一个指向对象的 <code>shared_ptr</code> 被销毁，对象就会被释放。但控制块本身直到 <code>weak_ptr</code> 计数器同样归零时才会释放。</p>
<p>当<code>weak_ptr</code>需要获得临时所有权时，需要将其转换为 <code>shared_ptr</code>，此时如果原来的 <code>shared_ptr</code> 被销毁，则该对象的生命期将被延长至这个临时的 <code>shared_ptr</code> 同样被销毁为止。</p>
<p>最好的设计是尽可能<strong>避免共享</strong>指针的所有权。但是，如果必须共享 <code>shared_ptr</code> 实例的所有权，则应避免在它们之间循环引用。</p>
<h4 id="circular-references"><a href="#circular-references" class="headerlink" title="circular references"></a>circular references</h4><p>在基于引用计数的任何类型的系统中，对类的引用可以形成循环，即第一个对象引用第二个对象，第二个对象引用第三个对象，依此类推，直到某个最终对象引用回第一个对象。因此，引用计数永远不会达到0，对象也永远不会被释放。</p>
<img src="/2020/08/12/pointer/cyclic.png" alt="cyclic reference" style="zoom: 80%;" />

<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="comment">//Example</span></span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">A</span></span></span><br><span class="line"><span class="class">&#123;</span></span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">    <span class="built_in">shared_ptr</span>&lt;B&gt; b_ptr;</span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">B</span></span></span><br><span class="line"><span class="class">&#123;</span></span><br><span class="line"><span class="keyword">public</span>:    </span><br><span class="line">    <span class="built_in">shared_ptr</span>&lt;A&gt; a_ptr;</span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">main</span><span class="params">()</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    <span class="keyword">auto</span> obj1 = make_shared&lt;A&gt;(<span class="keyword">new</span> A());</span><br><span class="line">    <span class="keyword">auto</span> obj2 = make_shared&lt;B&gt;(<span class="keyword">new</span> B());</span><br><span class="line">    obj1-&gt;b_ptr = obj2;</span><br><span class="line">    obj2-&gt;a_ptr = obj1;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>当 <code>shared_ptr</code> 离开作用域时，引用计数仍然都是1，因此A和B对象不会被删除。</p>
<img src="/2020/08/12/pointer/cyclic_solution.png" alt="solution" style="zoom:67%;" />

<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="comment">//Solution</span></span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">A</span></span></span><br><span class="line"><span class="class">&#123;</span></span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">    <span class="built_in">shared_ptr</span>&lt;B&gt; b_ptr;</span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">B</span></span></span><br><span class="line"><span class="class">&#123;</span></span><br><span class="line"><span class="keyword">public</span>:    </span><br><span class="line">    weak_ptr&lt;A&gt; a_ptr;</span><br><span class="line">    <span class="function"><span class="keyword">void</span> <span class="title">useA</span><span class="params">()</span></span></span><br><span class="line"><span class="function">    </span>&#123;</span><br><span class="line">        <span class="keyword">auto</span> a = a_ptr.lock();</span><br><span class="line">        <span class="keyword">if</span>(a) &#123;	<span class="comment">//<span class="doctag">TODO:</span> do something to a &#125;</span></span><br><span class="line">        <span class="keyword">else</span> &#123; <span class="comment">//<span class="doctag">TODO:</span> a has been deleted &#125;</span></span><br><span class="line">    &#125;</span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">main</span><span class="params">()</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    <span class="keyword">auto</span> obj1 = make_shared&lt;A&gt;(<span class="keyword">new</span> A());</span><br><span class="line">    <span class="keyword">auto</span> obj2 = make_shared&lt;B&gt;(<span class="keyword">new</span> B());</span><br><span class="line">    obj1-&gt;b_ptr = obj2;</span><br><span class="line">    obj2-&gt;a_ptr = obj1;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h3 id="References"><a href="#References" class="headerlink" title="References"></a>References</h3><p><a href="https://docs.microsoft.com/en-us/cpp/cpp/smart-pointers-modern-cpp?view=vs-2019" target="_blank" rel="noopener">Smart pointers (Modern C++) | Microsoft Docs</a></p>
<p><a href="https://en.cppreference.com/w/cpp/memory" target="_blank" rel="noopener">Dynamic memory management | cppreference.com</a></p>
<p><a href="https://docs.huihoo.com/actor-framework/en/ReferenceCounting.html" target="_blank" rel="noopener">Reference Counting | CAF </a></p>
]]></content>
      <categories>
        <category>Concepts</category>
      </categories>
      <tags>
        <tag>C/C++</tag>
      </tags>
  </entry>
  <entry>
    <title>Notes for OSTEP</title>
    <url>/2020/08/03/ostep-1/</url>
    <content><![CDATA[<h1 id="Operating-Systems-Three-Easy-Pieces"><a href="#Operating-Systems-Three-Easy-Pieces" class="headerlink" title="Operating Systems: Three Easy Pieces"></a><em>Operating Systems: Three Easy Pieces</em></h1><blockquote>
<p>Notes    -    Part One</p>
<p>Links to the book: <a href="http://pages.cs.wisc.edu/~remzi/OSTEP/" target="_blank" rel="noopener">http://pages.cs.wisc.edu/~remzi/OSTEP/</a></p>
</blockquote>
<h2 id="Introduction"><a href="#Introduction" class="headerlink" title="Introduction"></a>Introduction</h2><blockquote>
<p>What an OS actually does</p>
</blockquote>
<p>It takes physical resources, such as a CPU, memory, or disk, and <strong>virtualizes</strong> them. It handles tough and tricky issues related to <strong>concurrency</strong>. And it stores files <strong>persistently</strong>, thus making them safe over the long-term.</p>
<blockquote>
<p>Design goal</p>
</blockquote>
<ul>
<li>build up some <strong>abstractions</strong> in order to make the system convenient and easy to use</li>
<li>provide high <strong>performance</strong> / <strong>minimize the overheads</strong> of the OS</li>
<li>provide <strong>protections</strong> between applications, as well as the OS and applications.</li>
<li>strive to provide a high degree of <strong>reliability</strong>. ( run non-stop )</li>
<li>energy-efficiency / security / mobility…</li>
</ul>
<blockquote>
<p>History of how OS developed</p>
</blockquote>
<ol>
<li><p>Just Libraries: a set of libraries of commonly-used functions</p>
<p>Batch Processing: a number of jobs were set up and then run in a “batch” by the operator</p>
</li>
<li><p>Protection</p>
<p>if allow any program read any file?    -&gt;    the code run on behalf of the OS was special    -&gt;    <strong>system call</strong> </p>
<blockquote>
<p>Key difference between a system call and a procedure call</p>
<p>A system call transfer control into the OS while simultaneously raising the <strong>hardware privilege level</strong>. User applications run in what is referred to an <strong>user mode</strong> which means the hardware restricts what applications can do.</p>
<p>When a system call is initiated( usually through a special hardware instruction called a <strong>trap</strong>), the hardware transfers control to a pre-specified <strong>trap handler</strong>( that the OS set up previously) and simultaneously raises the privilege level to <strong>kernel mode</strong>( In kernel mode, the OS has full access to the hardware of system). When the OS is done servicing the request, it passed control back to the user via a special <strong>return-from-trap</strong> instruction, which reverts to user mode while simultaneously passing control back to where the application let off. </p>
</blockquote>
</li>
<li><p>Era of Multiprogramming</p>
<p>desire to make better use of machine resources    -&gt;    </p>
<p><strong>multiprogramming</strong>: load a number of jobs into memory and switch rapidly between them for improving CPU utilization    -&gt;</p>
<p>don’t want one program to be able to access the memory of another program    -&gt;</p>
<p><strong>memory protection</strong> &amp; other <strong>concurrency</strong> issues</p>
</li>
<li><p>Modern Era</p>
</li>
</ol>
<h2 id="Virtualization"><a href="#Virtualization" class="headerlink" title="Virtualization"></a>Virtualization</h2><h3 id="The-Abstraction-The-Process"><a href="#The-Abstraction-The-Process" class="headerlink" title="The Abstraction: The Process"></a>The Abstraction: The Process</h3><p>The definition of a process    -&gt;    ( informally ) a running program</p>
<p>A program    -&gt;    a lifeless thing: sits on the disk, a bunch of instructions and maybe some static data, waiting to spring into action</p>
<img src="/2020/08/03/ostep-1/process.png" alt="process" style="zoom:60%;" />

<blockquote>
<p>Summarize a process by taking an inventory of the different pieces of the system it accesses or affects</p>
</blockquote>
<ul>
<li><p>the memory that the process can address</p>
<p>Instructions / the data it reads or writes sits in memory</p>
</li>
<li><p>registers、</p>
<p>Many instructions explicitly read or update registers. (includes some special registers like IP/SP…)</p>
</li>
<li><p>access persistent storage devices</p>
<p>Might include a list of the files it currently has open.</p>
</li>
</ul>
<blockquote>
<p>Process creation</p>
</blockquote>
<ol>
<li><p>load its code and any static data (e.g., initialized variables) into memory, into the address space of the process</p>
<p>from eager (Early OS: <strong>all at once</strong> before running) to lazy (Modern OS: <strong>load pieces</strong> when needed during execution)</p>
</li>
<li><p>allocate memory for program’s <strong>run-time stack</strong></p>
</li>
<li><p>allocate memory for program’s <strong>heap</strong></p>
</li>
<li><p>other work as related to <strong>I/O setup</strong></p>
<p>e.g., in Unix systems, each process by default has 3 open file descriptors for standard input/output/error.</p>
</li>
</ol>
<blockquote>
<p>Process state in a simplified view</p>
</blockquote>
<img src="/2020/08/03/ostep-1/process_state.png" alt="process state" style="zoom:60%;" />

<ul>
<li>running: runing on a processor and executing instructions.</li>
<li>ready: ready to run but for some reason the OS has chosen not to run it at this given moment. </li>
<li>blocked: has performed some kind of operation that makes it not ready to run until some other event takes place </li>
</ul>
<p>other state:</p>
<ul>
<li>initial: begin created</li>
<li>final: has exited but has not yet been cleaned up (zombie state)</li>
</ul>
<blockquote>
<p>Key data structures to track information</p>
</blockquote>
<p>track state of each process     -&gt;    process list</p>
<p>individual structure stores information about a process    -&gt;    process control block:</p>
<img src="/2020/08/03/ostep-1/pcb.png" alt="PCB" style="zoom: 33%;" />

<blockquote>
<p>How can the OS provide the illusion of a nearly-endless supply of CPUs</p>
</blockquote>
<p>By <strong>virtualizing</strong> the CPU. By running one process, then stopping it and running another, and so forth, it can promote the illusion.</p>
<p><strong>Mechanisms</strong></p>
<blockquote>
<p>low-level methods or protocols that implement a needed piece of functionality.    -&gt;     how</p>
</blockquote>
<ul>
<li><p>Time sharing</p>
<p>It is a basic technique used by an OS to share a resource. By allowing the resource to be used for a little while by one entity, and then a little while by another, and so forth, the resources in question can be shared by many.</p>
</li>
<li><p>Context switch</p>
<p>Give the OS the ability to stop running one program and start running another on a given CPU.</p>
</li>
</ul>
<p><strong>Policies</strong></p>
<blockquote>
<p>On top of the mechanisms (high-level), algorithms for making some kind of decision    -&gt;    which</p>
</blockquote>
<ul>
<li><p>scheduling policy</p>
<p>Determine which program should the OS run.</p>
</li>
</ul>
<h3 id="Key-Process-API"><a href="#Key-Process-API" class="headerlink" title="Key Process API"></a>Key Process API</h3><ul>
<li>The <strong>fork()</strong> system call is used in Unix systems to create a new process. The creator is called the <strong>parent</strong>; the newly created process is called the <strong>child</strong>.</li>
<li>The <strong>wait()</strong> system call allows a parent to wait for its child to complex execution.</li>
<li>The <strong>exec()</strong> family of system calls allows a child to break free from its similarity to its parent and execute an entirely new program.</li>
</ul>
<blockquote>
<p>Why the separation of <strong>fork()</strong> and <strong>exec()</strong> is essential in building a Unix shell</p>
</blockquote>
<p>It lets the shell run code <em>after</em> the call to <strong>fork()</strong> but <em>before</em> the call to <strong>exec()</strong>; this code can alter the environment of the about-to-be-run program, and thus enables a variety of interesting features to be readily built.</p>
<h3 id="Mechanism-Limited-Direct-Execution"><a href="#Mechanism-Limited-Direct-Execution" class="headerlink" title="Mechanism: Limited Direct Execution"></a>Mechanism: Limited Direct Execution</h3><blockquote>
<p>Challenges</p>
</blockquote>
<ul>
<li>performance    -     implement virtualization without adding excessive overhead to system</li>
<li>control    -    run process efficiently while retaining control over the CPU</li>
</ul>
<blockquote>
<p>Direct Execution Protocol (Without Limits)</p>
</blockquote>
<ol>
<li><p>OS:</p>
<p>Create entry for process list    -&gt;    Allocate memory for program    -&gt;    Load program into memory    -&gt;    Set up stack with argc / argv    -&gt;    Clear registers    -&gt;    Execute call main()</p>
</li>
<li><p>Program:</p>
<p>Run main    -&gt;    Execute return from main</p>
</li>
<li><p>OS:</p>
<p>Free memory of process    -&gt;    Remove from process list</p>
</li>
</ol>
<p>Advantage: fast</p>
<p>Problems:</p>
<ol>
<li><p>Restricted Operations</p>
<blockquote>
<p>A process must be able to perform I/O and some other restricted operations, but without giving the process complete control over the system.</p>
</blockquote>
<p>Approach: a new process mode</p>
<p>Code that runs in <strong>user mode</strong> is <strong>restricted</strong> in what it can do. In contrast to user mode is <strong>kernel mode</strong>, which the operating system (or kernel) runs in. In this mode, code that runs can <strong>do what it likes</strong>, including privileged operations.</p>
<p>To make a user process perform some kind of privileged operation when it wishes, the <strong>system call</strong> is provided. To execute a system call, a program must execute a special <strong>trap</strong> instruction. This instruction simultaneously jumps into the kernel and raises the privilege level to <strong>kernel mode</strong>; once in kernel, the system can now perform whatever privileged operations are needed, and thus <strong>do the required work for the calling process</strong>. When finished, the OS calls a special <strong>return-from-trap</strong> instruction to return into the calling user program while simultaneously reducing the privilege level <strong>back to user mode</strong>.</p>
<p>In order to be able to return correctly when the OS issues the return-from-trap instruction, it must make sure to <strong>save enough of the caller’s registers</strong>. For example, on x86, the processor will push them onto <strong>per-process kernel stack</strong> and the return-from-trap will pop them off and resume execution of the user-mode process.</p>
</li>
</ol>
<blockquote>
<p>How does the trap know which code to run inside the OS</p>
</blockquote>
<p>   Clearly, if the calling process does this by <strong>specifying an address to jump to</strong>, it will allow programs to jump anywhere into the kernel which is a <strong>very bad idea</strong>.</p>
<p>   The kernel does so by setting up a trap table at boot time. When the machine boot up, it does so in privileged (kernel) mode, and thus is free to configure machine hardware as need be.</p>
<p>   One of the first things the OS thus does is to tell the hardware what code to run (<strong>location of trap handlers</strong>) when certain exceptional events occur. Once the hardware is informed, it remembers the location of these handlers until the machine is <strong>next rebooted</strong>.</p>
<p>   To specify the exact system call, a <strong>system-call number</strong> is usually assigned to each system call. The user code is thus responsible for placing the desired system-call number in a <strong>register</strong> or at a specified location on the <strong>stack</strong>; the OS,when handling the system call inside the trap handler, <strong>examines this number</strong>, ensures it is valid, and if it is, executes the corresponding code.</p>
<p>   One last aside: being able to execute the instruction to tell the hardware <strong>where the trap tables are</strong> is also a <strong>privileged operation</strong>.</p>
<blockquote>
<p>Limited Direct Execution Protocol (two phases)</p>
</blockquote>
<p>   In the first (at boot time), the kernel initializes the trap table, and the CPU remembers its location for subsequent use. The kernel does so via a privileged instruction (highlighted).</p>
   <img src="/2020/08/03/ostep-1/lde_1.png" alt="lde1" style="zoom:60%;" />

<p>   In the second (when running a process), <strong>the kernel sets up</strong> a few things such as allocation before using a return-from-trap instruction to start the execution of the process; this <strong>switches the CPU to user mode</strong> and begins running the process. When the process wishes to <strong>issue a system call</strong>, it <strong>traps back</strong> into the OS, which <strong>handles</strong> it and once again <strong>return control via a return-from-trap</strong> to the process. The process then completes its work, and returns from main(); this usually will return into some stub code which will properly <strong>exit the program (by calling the exit() system call)</strong>. At this point, the OS cleans up.</p>
   <img src="/2020/08/03/ostep-1/lde_2.png" alt="lde" style="zoom:60%;" />

<ol start="2">
<li><p>Switching Between Process</p>
<blockquote>
<p>If a process is running on the CPU, this by definition means the OS is not running. So how can the operating system <strong>regain control</strong> of the CPU so that it can switch between process?</p>
</blockquote>
<p><strong>A cooperative approach: wait for system calls</strong></p>
<p>In this style, the OS <strong>trusts the processes</strong> of the system to <strong>behave reasonably</strong>. Processes that run for too long are assumed to periodically give up the CPU so that the OS can decide to run some other task.</p>
<p>In a cooperative scheduling system, the OS regains control of the CPU by <strong>waiting for a system call or an illegal operation</strong> of some kind to take place. This passive approach is too ideal.</p>
</li>
</ol>
<p>   <strong>A non-cooperative approach: the OS takes control</strong></p>
<p>   Without some additional help from the hardware, it turns out that the OS can’t do much at all when a process refuses to make system calls (or mistakes) and thus return control to the OS.</p>
<p>   The answer to how to gain control <strong>without cooperation</strong> is simple: a <strong>timer interrupt</strong>. A timer device can be programmed to raise an interrupt every so many milliseconds; when the interrupt is raised,the currently running process is <strong>halted</strong>, and a <strong>pre-configured interrupt handler</strong> in the OS runs. At this point, the OS has regained control of the CPU, and thus can do what it pleases: stop the current process, and start a different one.</p>
   <img src="/2020/08/03/ostep-1/timer.png" alt="timer" style="zoom:60%;" />

<blockquote>
<p>Saving and Restoring Context</p>
</blockquote>
<p>   It is made by a part of the operating system known as the scheduler to decide whether to continue running the currently-running process, or switch to a different one.</p>
<p>   If the decision is made to switch, the OS then executes a low-level piece of code which we refer to as a <strong>context switch</strong>. A context switch is conceptually simple: all the OS has to do is <strong>save a few register values</strong> for the currently-running process (onto its kernel stack, for example), and <strong>restore a few</strong> (from its kernel stack), and <strong>switch to the kernel stack</strong> for the soon-to-be-executing process. By switching stacks, the kernel enters the call to the switch code in the context of one process (the one that was interrupted) and returns in the context of another (the soon-to-be-executing one). When the OS finally executes a return-from-trap instructions, the soon-to-be-executing process becomes the currently-running process. And thus the context switch is complete.</p>
   <img src="/2020/08/03/ostep-1/switch.png" alt="context switch" style="zoom:60%;" />

<p>   two types of register saves/restores</p>
<ol>
<li>When <strong>interrupt occurs</strong>, the <strong>user registers</strong> of the running process are implicitly saved by the <strong>hardware</strong>, using the <strong>kernel stack</strong> of that process.</li>
<li>When the OS <strong>decides to switch</strong>, the <strong>kernel registers</strong> are explicitly saved by the <strong>software</strong> (i.e., the OS) , into memory in the <strong>process structure</strong> of the process.</li>
</ol>
<blockquote>
<p>What happen if, during interrupt or trap handling, another interrupt occurs. (Detailed discussion is defer until second piece)</p>
</blockquote>
<p>One simple thing an OS might do is <strong>disable interrupts</strong> during interrupt processing; doing so ensures that when one interrupt is being handled, no other one will be delivered to the CPU. But disabling interrupts for too long could <strong>lead to lost interrupts</strong>.</p>
<p>Operating systems also have developed a number of <strong>sophisticated locking schemes</strong> to protect concurrent access to internal data structures. This enables <strong>multiple activities to be on-going</strong> within the kernel at the same time, particularly useful on multiprocessors.</p>
]]></content>
      <categories>
        <category>Notes</category>
      </categories>
      <tags>
        <tag>Operating System</tag>
      </tags>
  </entry>
  <entry>
    <title>handy源码分析</title>
    <url>/2020/04/18/handy/</url>
    <content><![CDATA[<h1 id="Handy网络库分析"><a href="#Handy网络库分析" class="headerlink" title="Handy网络库分析"></a>Handy网络库分析</h1><p>最近想学一下Linux的网络编程，于是想先阅读一些网络库，所以这篇博客可能会有一些比较基本的知识点。</p>
<p>直接开始，我们先来看看利用Doxygen生成的Include依赖关系图</p>
<img src="/2020/04/18/handy/includes.png" class="" title="include-relationship">

<p>我们就按从下往上来分析一下各个头文件所实现的功能</p>
<h2 id="slice-h"><a href="#slice-h" class="headerlink" title="slice.h"></a>slice.h</h2><p>定义字符串切片类（包含front() / back() / begin() / end() 等类似容器的操作，但实际的实现是char*）以及切片的一些操作（返回第一个词，返回第一行，返回前n个字符等等）</p>
<h2 id="port-posix-h"><a href="#port-posix-h" class="headerlink" title="port_posix.h"></a>port_posix.h</h2><p>用于解决主机字节序到网络字节序的转换，根据 OS_LINUX / OS_MACOSX 实现不同的getHostByName</p>
<h2 id="util-h"><a href="#util-h" class="headerlink" title="util.h"></a>util.h</h2><blockquote>
<p>这里noncopyable是通过将<strong>拷贝构造函数</strong>和<strong>拷贝赋值运算符</strong>声明为禁止使用(=delete)实现</p>
<p>这样实现的好处：</p>
<ul>
<li>在c++11之前实现该功能需要将这两个函数设为private，但依然无法避免成员函数和友元函数的使用</li>
<li>通过更少/更明确的代码实现，且可复用(从noncopyable类派生)</li>
</ul>
<p>该类的作用：</p>
<ul>
<li>防止对象被拷贝。尤其是在涉及到文件操作/网络连接的类中，资源的释放/关闭可能会出现问题</li>
</ul>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="comment">//boost::noncopyable</span></span><br><span class="line"><span class="class"><span class="keyword">struct</span> <span class="title">noncopyable</span></span></span><br><span class="line"><span class="class">&#123;</span></span><br><span class="line">  noncopyable() =<span class="keyword">default</span>;</span><br><span class="line">  </span><br><span class="line">  noncopyable(<span class="keyword">const</span> noncopyable&amp;) =<span class="keyword">delete</span>;</span><br><span class="line">  noncopyable&amp; <span class="keyword">operator</span>=(<span class="keyword">const</span> noncopyable&amp;) =<span class="keyword">delete</span>;</span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure>

<p>具体解释可看<a href="https://docs.microsoft.com/en-us/cpp/cpp/explicitly-defaulted-and-deleted-functions?view=vs-2019" target="_blank" rel="noopener">Explicitly Defaulted and Deleted Functions</a></p>
</blockquote>
<p>util类则包含</p>
<ul>
<li><p>格式化输出string</p>
<blockquote>
<p>进行两次尝试：</p>
<p>第1次尝试：</p>
<p>申请char[500]的栈内存，va_start获取可变参数列表，通过vsnprintf()得到格式化后的字符串，判断返回大小是否超出限制，未超出结束尝试并返回，超出则进行第二次尝试</p>
<p>第2次尝试：</p>
<p>申请char[30000]的堆内存，并且由<strong>unique_ptr</strong>管理(销毁)，通过vsnprintf()得到格式化后的字符串，判断返回大小是否超出限制，未超出结束尝试并返回，超出则截断，将字符串最后一字节设为结束字符‘\0’</p>
</blockquote>
</li>
<li><p>基础的时间函数（std::chrono库）</p>
</li>
<li><p>atoi() —- 将字符串转换为整数，函数内部通过strtol()实现</p>
</li>
<li><p>更改(添加)文件描述符flag（fcntl）</p>
</li>
</ul>
<p>ExitCaller类继承于noncopyable，只有一个私有成员<code>std::function&lt;void()&gt; functor_</code>，即一个无参数返回int的可调用对象，并且在析构时进行调用执行。当实例为局部变量时，作用与golang的defer类似</p>
<h2 id="net-h"><a href="#net-h" class="headerlink" title="net.h"></a>net.h</h2><p><code>net</code>类包含一些设置函数</p>
<ul>
<li><p>setNonBlock()设置文件的 阻塞/非阻塞IO</p>
</li>
<li><p>setReuseAddr() / setReusePort() / setNoDelay() 设置套接字的属性</p>
<blockquote>
<p><code>SO_REUSEADDR</code>允许 绑定一个正处于TIME_WAIT中的本地地址 / 通配符IP(INADDR_ANY)地址冲突</p>
<p><code>SO_REUSEPORT</code>允许多个套接字绑定到同一地址端口组合(所有套接字必须都设置了<code>SO_REUSEPORT</code>)</p>
<p><code>TCP_NODELAY</code> 关闭Nagle缓存算法，允许小包的发送(适合延时敏感且数据量较小的情况)</p>
<p><a href="http://man7.org/linux/man-pages/man7/socket.7.html" target="_blank" rel="noopener">man socket</a></p>
<p><a href="https://stackoverflow.com/questions/14388706/how-do-so-reuseaddr-and-so-reuseport-differ" target="_blank" rel="noopener">How do SO_REUSEADDR and SO_REUSEPORT differ?</a></p>
</blockquote>
</li>
</ul>
<p><code>Ip4Addr</code>类则是对sockaddr_in进行了一些简单的封装，提供一些常用的函数(format/getter/validate)</p>
<p><code>Buffer</code>类是连续的动态伸缩的内存空间，用于缓存</p>
<p>内存空间大小由私有成员变量</p>
<p><code>char *buf_</code>内存地址起始地址 / <code>size_t b_</code>正在使用内存空间（缓存数据但未使用）的起始地址相对buf的偏移量 / <code>size_t e_</code>正在使用内存空间的结束地址 + 1数据单元地址相对buf的偏移量 / <code>size_t cap_</code>已分配空间大小 / <code>size_t exp_</code>建议/期望分配大小</p>
<ul>
<li><p>添加数据：先调用<code>allocRoom(len)</code>分配足够大小空间，然后<code>memcpy()</code>拷贝数据</p>
</li>
<li><p>分配空间：调用<code>makeRoom(len)</code>获得所分配空间的首地址</p>
<p>在分配过程中，如果内存空间足够(<code>e_ + len &lt;= cap_</code>)，无需操作</p>
<p>如果内存尾部空间不足</p>
<ol>
<li>判断当前所需要使用的内存是否小于已分配的一半(<code>size() + len &lt; cap_/2</code>)，是则将有效数据移动到起始位置<code>buf_</code></li>
<li>调用<code>expand(len)</code>重新分配一块内存，内存大小为<code>max(exp_, max(2 * cap_, size() + len))</code></li>
</ol>
</li>
<li><p>获取数据：<code>consume(size_t len)</code>直接<code>b_ += len</code>，所以使用时需保证<code>len</code>的大小正确</p>
</li>
</ul>
<h2 id="codec-h"><a href="#codec-h" class="headerlink" title="codec.h"></a>codec.h</h2><blockquote>
<p>TCP的粘包/拆包问题：TCP是字节流协议，消息之间没有边界</p>
<p>一般的处理：</p>
<ul>
<li>发送定长包。如果每个消息的大小都是一样的，那么在接收对等方只要累计接收数据，直到数据等于一个定长的数值就将它作为一个消息。</li>
<li>包尾加上\r\n标记。FTP协议正是这么做的。但问题在于如果数据正文中也含有\r\n，则会误判为消息的边界。</li>
<li>包头加上包体长度。包头是定长的4个字节，说明了包体的长度。接收对等方先接收包体长度，依据包体长度来接收包体。</li>
<li>使用更加复杂的应用层协议</li>
</ul>
</blockquote>
<p><code>CodecBase</code>类 就是用来处理这些问题，其派生类<code>LineCodec</code>采用第二种方案，<code>LengthCodec</code>采用第三种方案</p>
<p><code>LengthCodec</code>类的具体编码：”mBdt”（四个字节）+ 消息长度（四个字节）+消息</p>
<h2 id="threads-h"><a href="#threads-h" class="headerlink" title="threads.h"></a>threads.h</h2><p>SafeQueue类</p>
<p>要做到线程安全，我们需要利用锁。一般在封装线程安全的类时，我们会直接声明一个mutex成员变量，但是该类是通过私有继承mutex类实现，所以上锁的时候直接传入(*this)：<code>lock_guard&lt;mutex&gt; lk(*this)</code></p>
<p><code>push()</code>任务时直接上锁，然后将任务添加到链表中，调用<code>notify_one()</code>唤醒一个等待条件变量<code>ready_</code>的线程</p>
<p><code>wait_ready()</code>使用条件变量阻塞线程，直到 超时 / 队列中不为空(有任务) / 退出</p>
<p><code>pop_wait()</code>调用<code>wait_ready()</code>，返回空任务(退出/超时)或者队列头任务(队列不为空)</p>
<p>ThreadPool类</p>
<p>构造一个最大等待任务数量为<code>taskCapacity</code>，线程数量为<code>thread</code>的线程池，当线程启动时，遍历线程集合，对于每一个线程，创建一个新的线程执行<code>pop_wait()</code>获取任务并执行，然后交换，即将新线程替换旧线程</p>
<h2 id="logging-h"><a href="#logging-h" class="headerlink" title="logging.h"></a>logging.h</h2><p>定义了8个日志等级</p>
<p><code>enum LogLevel { LFATAL = 0, LERROR, LUERR, LWARN, LINFO, LDEBUG, LTRACE, LALL };</code></p>
<p>Logger类是通过<code>getLogger()</code>获取一个静态单例（c++11保证局部静态变量的初始化是线程安全的），功能</p>
<ol>
<li><p>可以绝对<code>setLogLevel()</code> / 相对<code>adjustLogLevel()</code>设置 / 获取<code>getLogLevel()</code>当前的日志输出等级</p>
</li>
<li><p>自定义设置日志文件，打开方式</p>
<ul>
<li><p><code>O_APPEND</code>将写入追加到文件的尾端</p>
</li>
<li><p><code>O_CREAT</code>不存在则自动创建，权限为<code>#define DEFFILEMODE (S_IRUSR|S_IWUSR|S_IRGRP|S_IWGRP|S_IROTH|S_IWOTH)</code> 即 User/Group/Others可读可写</p>
</li>
<li><p><code>O_WRONLY</code>只写</p>
</li>
<li><p><code>O_CLOEXEC</code>设置文件描述符<code>close-on-exec</code>，即当fork()的子进程成功调用exec()类函数时，会自动关闭该文件描述符，避免泄露给子进程</p>
</li>
</ul>
<blockquote>
<p>修改成员函数的时候使用的是<code>dup2()</code>，避免<code>close()</code>+<code>dup()</code>两个函数会受到竞争条件的影响：比如在两个函数之间，成员函数被赋值为另一个fd</p>
</blockquote>
</li>
<li><p>设置日志周期<code>rotateInterval_</code></p>
<ul>
<li>如果当前时间与当前日志文件创建时间在同一周期内，无需新建日志文件</li>
<li>否则，保存并重命名为 当前年月日时分 </li>
</ul>
</li>
<li><p>写入日志<code>logv(int level, const char *file, int line, const char *func, const char *fmt...)</code></p>
<ul>
<li>如果写入日志等级高于当前日志等级，退出</li>
<li>检查是否开启新的日志周期（重命名）</li>
<li>写入详细的时间 + tid + 发生的文件/位置 + 事件内容 + ‘\n’</li>
</ul>
</li>
</ol>
<h2 id="status-h"><a href="#status-h" class="headerlink" title="status.h"></a>status.h</h2><p>表示文件操作后的状态</p>
<p>表示格式：</p>
<p>state_[0..3] == length of state_</p>
<p>state_[4..7] == code</p>
<p>state_[8..]  == message</p>
<h2 id="file-h"><a href="#file-h" class="headerlink" title="file.h"></a>file.h</h2><p>file类中包含了一些文件io的静态成员函数，返回<code>Status</code>类</p>
<ul>
<li><p><code>getContent(const string &amp;filename, string &amp;cont)</code>：将文件内容读(append)到字符串中</p>
</li>
<li><p><code>writeContent(const string &amp;filename, const string &amp;cont)</code>：截断并从头往文件写入字符串中内容</p>
</li>
<li><p><code>createDir(const string &amp;name)</code>/ <code>deleteDir(const string &amp;name)</code>：创建/删除文件目录</p>
</li>
<li><p><code>getChildren(const string &amp;dir, vector&lt;string&gt; *result)</code>：通过<code>readdir()</code>获取遍历目录文件信息，然后它们的文件名 <code>push_back(d_name)</code>到<code>*result</code></p>
</li>
<li><p><code>getFileSize((const string &amp;fname, uint64_t *size))</code>：通过调用<code>stat(const char *path, struct stat *buf)</code> 获取文件大小，并将<code>*size</code>赋值为文件大小<code>buf.st_size</code></p>
</li>
<li><p><code>deleteFile(const string &amp;fname)</code>：通过调用<code>unlink(const char *pathname)</code>删除文件</p>
<blockquote>
<p><strong>unlink</strong>() 从文件系统删除一个名字.</p>
<ul>
<li>如果这个名字是指向文件的最后一个链接并且该文件当前没有被某进程打开，则删除该文件</li>
<li>如果这个名字是指向文件的最后一个链接并且该文件正在被某进程打开，则等待指向该文件的最后一个文件描述符被关闭时删除该文件</li>
<li>如果这个名字指向一个软链接，则删除该软连接</li>
<li>如果名称指向一个socket、fifo或设备，则删除其名称，但打开该对象的进程可以继续使用它。</li>
</ul>
</blockquote>
</li>
<li><p>调用<code>writeContent(tmpName,cont)</code> 往 <code>tmpName</code>文件写入内容成功后，调用<code>renameFile(tmpName,name)</code>重命名</p>
</li>
</ul>
<h2 id="handy-imp-h"><a href="#handy-imp-h" class="headerlink" title="handy_imp.h"></a>handy_imp.h</h2><p><code>AutoContext</code>类通过模板类实现对传入类的内存管理，自动（第一次访问时）new和（析构时）delete</p>
<h2 id="poller-h"><a href="#poller-h" class="headerlink" title="poller.h"></a>poller.h</h2><blockquote>
<p>结合event_base一起看</p>
</blockquote>
<p>基于<code>PollerBase</code>抽象类，根据Linux和MacOS不同平台派生类，实现了对epoll的封装</p>
<h3 id="epoll"><a href="#epoll" class="headerlink" title="epoll"></a>epoll</h3><blockquote>
<p><code>epoll</code>把用户关心的文件描述符上的事件放在内核里的一个事件表中，从而无需像 <code>select</code> 和 <code>poll</code> 那样每次调用都要重复传入文件描述符集或事件集。但 <code>epoll</code> 需要使用一个额外的文件描述符来<strong>唯一标识</strong>内核中的这个事件表</p>
</blockquote>
<p>这里我们先来看看epoll的几个函数</p>
<ul>
<li><p><code>int epoll_create(int size)</code>：用来创建上述的文件描述符。<code>size</code>参数现在不起作用，只是给内核一个提示，告诉它事件表需要多大</p>
</li>
<li><p><code>int epoll_ctl(int epfd, int op, int fd, struct epoll_event *event)</code>：</p>
<p><code>fd</code> 是要操作的文件描述符，<code>op</code> 参数则指定操作类型，有如下3种：</p>
<ul>
<li><code>EPOLL_CTL_ADD</code>：往事件表种注册 <code>fd</code> 上的事件</li>
<li><code>EPOLL_CTL_MOD</code>：修改 <code>fd</code> 上的注册事件</li>
<li><code>EPOLL_CTL_DEL</code>：删除 <code>fd</code> 上的注册事件</li>
</ul>
<p><code>event</code> 参数指定事件，<code>epoll_event</code> 结构体定义了：<code>events</code> 成员描述事件类型，<code>data</code> 成员存储用户数据</p>
</li>
<li><p><code>int epoll_wait(int epfd, struct epoll_event* events, int maxevents, int timeout)</code>：</p>
<p><code>epoll_wait()</code>在一段超时时间内等待一组文件描述符上的事件，<code>timeout</code> 指定超时值，单位是ms，<code>maxevents</code> 指定最多监听多少个事件</p>
<p><code>epoll_wait()</code> 如果检测到事件，就将所有就绪的事件从内核事件表中复制它的第二个参数<code>events</code>指向的数组中</p>
</li>
</ul>
<h4 id="Level-Trigger"><a href="#Level-Trigger" class="headerlink" title="Level Trigger"></a>Level Trigger</h4><blockquote>
<p>当<code>epoll_wait()</code> 检测到有事件发生并将其通知应用程序后，应用程序可以不立即处理该事件。当应用程序下一次调用 <code>epoll_wait()</code> 时，此事件仍然会被通知，直到该事件被处理</p>
</blockquote>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">lt</span><span class="params">(epoll_event* events, <span class="keyword">int</span> number, <span class="keyword">int</span> epollfd, <span class="keyword">int</span> listenfd)</span></span>&#123;</span><br><span class="line">    <span class="keyword">char</span> buf[BUFFER_SIZE];</span><br><span class="line">    <span class="keyword">for</span>(<span class="keyword">int</span> i = <span class="number">0</span>; i &lt; number; i++)&#123;</span><br><span class="line">        <span class="keyword">int</span> sockfd = events[i].data.fd;</span><br><span class="line">        <span class="keyword">if</span>(sockfd == listenfd) &#123;	<span class="comment">/*Regist*/</span>	&#125;</span><br><span class="line">        <span class="keyword">else</span> <span class="keyword">if</span> (events[i].events &amp; EPOLLIN)&#123;</span><br><span class="line">            <span class="keyword">int</span> ret = recv( sockfd, buf, BUFFER_SIZE - <span class="number">1</span>, <span class="number">0</span>);</span><br><span class="line">            <span class="keyword">if</span>(ret &lt;= <span class="number">0</span>)&#123;</span><br><span class="line">                <span class="built_in">close</span>(sockfd);</span><br><span class="line">                <span class="keyword">continue</span>;</span><br><span class="line">            &#125;</span><br><span class="line">            <span class="comment">//data processing</span></span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">else</span> &#123;&#125;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h4 id="Edge-Trigger"><a href="#Edge-Trigger" class="headerlink" title="Edge Trigger"></a>Edge Trigger</h4><blockquote>
<p>当<code>epoll_wait()</code> 检测到有事件发生并将其通知应用程序后，应用程序必须立即处理该事件，因为后续的<code>epoll_wait()</code> 不再向应用程序通知该事件</p>
</blockquote>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">et</span><span class="params">(epoll_event* events, <span class="keyword">int</span> number, <span class="keyword">int</span> epollfd, <span class="keyword">int</span> listenfd)</span></span>&#123;</span><br><span class="line">    <span class="keyword">char</span> buf[BUFFER_SIZE];</span><br><span class="line">    <span class="keyword">for</span>(<span class="keyword">int</span> i = <span class="number">0</span>; i &lt; number; i++)&#123;</span><br><span class="line">        <span class="keyword">int</span> sockfd = events[i].data.fd;</span><br><span class="line">        <span class="keyword">if</span>(sockfd == listenfd)&#123;		<span class="comment">/*Regist*/</span>	&#125;</span><br><span class="line">        <span class="keyword">else</span> <span class="keyword">if</span>(events[i].events &amp; EPOLLIN)&#123;</span><br><span class="line">            <span class="keyword">while</span>(<span class="number">1</span>)&#123;</span><br><span class="line">                <span class="keyword">int</span> ret = recv( sockfd, buf, BUFFER_SIZE - <span class="number">1</span>, <span class="number">0</span>);</span><br><span class="line">                <span class="keyword">if</span>(ret &lt; <span class="number">0</span>)&#123;</span><br><span class="line">                    <span class="keyword">if</span>( (errno == EAGAIN ) || (errno == EWOULDBLOCK))<span class="keyword">break</span>;</span><br><span class="line">                    <span class="built_in">close</span>(sockfd);</span><br><span class="line">                    <span class="keyword">break</span>;</span><br><span class="line">                &#125;</span><br><span class="line">                <span class="keyword">else</span> <span class="keyword">if</span>(ret == <span class="number">0</span>)<span class="built_in">close</span>(sockfd);</span><br><span class="line">                <span class="keyword">else</span> &#123;	<span class="comment">/*data processing*/</span>	&#125;</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">else</span>&#123;&#125;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h2 id="event-base-h"><a href="#event-base-h" class="headerlink" title="event_base.h"></a>event_base.h</h2><blockquote>
<p>事件的到来是随机的、异步的。我们无法预知程序何时收到一个客户连接请求 / 暂停信号。所以程序需要循环等待并处理事件，这就是<strong>事件循环</strong>。在事件循环中，等待事件一般使用I/O复用技术实现。</p>
<p>将系统支持的各种I/O复用系统调用封装成统一的接口，称为<strong>事件多路分发器</strong>，一般包含三个函数：等待事件(select/poll/epoll_wait)，添加事件，删除事件。</p>
<p><strong>事件处理器</strong>执行事件对应的业务逻辑，通常包含一个或多个回调函数，这些回调函数在事件循环中被执行。</p>
</blockquote>
<p><code>TimerRepeatable</code> 类是一个带有编号，回调函数的定时器，通过其中的 <code>at</code> 过期时间，<code>interval</code>定时周期控制</p>
<p><strong>事件循环</strong>：<code>EventBase::loop()</code> -&gt; <code>EventsImp::loop()</code> -&gt; <code>poller::loop_once()</code></p>
<p><strong>事件分发器</strong>：<code>EventBase::safeCall()</code> -&gt; <code>EventsImp::safeCall()</code> -&gt;</p>
<p><code>EventsBase</code>类是对 <code>EventsImp</code> 类再进一层封装，简化操作，方便多线程事件派发器<code>MultiBase</code>的实现</p>
<p> <code>EventsImp</code> 类成员</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line">EventBase *base_;		<span class="comment">//所属的事件分发器</span></span><br><span class="line">PollerBase *poller_;	</span><br><span class="line"><span class="built_in">std</span>::atomic&lt;<span class="keyword">bool</span>&gt; exit_;	<span class="comment">//设置退出</span></span><br><span class="line"><span class="keyword">int</span> wakeupFds_[<span class="number">2</span>];			<span class="comment">//用于唤醒IO线程</span></span><br><span class="line"><span class="keyword">int</span> nextTimeout_;			</span><br><span class="line">SafeQueue&lt;<span class="built_in">Task</span>&gt; tasks_;</span><br><span class="line"></span><br><span class="line"><span class="built_in">std</span>::<span class="built_in">map</span>&lt;TimerId, TimerRepeatable&gt; timerReps_;	<span class="comment">//循环定时任务</span></span><br><span class="line"><span class="built_in">std</span>::<span class="built_in">map</span>&lt;TimerId, <span class="built_in">Task</span>&gt; timers_;	<span class="comment">//定时任务</span></span><br><span class="line"><span class="built_in">std</span>::atomic&lt;<span class="keyword">int64_t</span>&gt; timerSeq_;		<span class="comment">//给每个定时器编号</span></span><br><span class="line"><span class="comment">// 记录每个idle时间（单位秒）下所有的连接。链表中的所有连接，最新的插入到链表末尾。连接若有活动，会把连接从链表中移到链表尾部，做法参考memcache</span></span><br><span class="line"><span class="built_in">std</span>::<span class="built_in">map</span>&lt;<span class="keyword">int</span>, <span class="built_in">std</span>::<span class="built_in">list</span>&lt;IdleNode&gt;&gt; idleConns_;	<span class="comment">//空闲连接</span></span><br><span class="line"><span class="built_in">std</span>::<span class="built_in">set</span>&lt;TcpConnPtr&gt; reconnectConns_;		<span class="comment">//重连连接</span></span><br><span class="line"><span class="keyword">bool</span> idleEnabled;			<span class="comment">//是否正在idle watcher模式</span></span><br><span class="line"></span><br><span class="line"><span class="comment">//调用poller::loop_once()，然后调用handleTimeouts()处理超时任务</span></span><br><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">loop_once</span><span class="params">(<span class="keyword">int</span> waitMs)</span></span>;</span><br><span class="line"><span class="comment">//循环调用loop_once(10000)直至设置退出，然后清空所有定时任务，清空连接，loop_once(0)</span></span><br><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">loop</span><span class="params">()</span></span>;</span><br><span class="line"><span class="comment">//创建管道，然后向poller注册它的可读事件，用于唤醒IO线程</span></span><br><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">init</span><span class="params">()</span></span>;</span><br><span class="line"></span><br><span class="line"><span class="comment">//如果连接空闲时间超过idle，重置该连接的空闲时间，将其从链表头部放到尾部，执行空闲超时回调</span></span><br><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">callIdles</span><span class="params">()</span></span>;</span><br><span class="line"><span class="comment">//启用idle watcher模式，将注册事件添加到对应idle秒的链表末尾</span></span><br><span class="line"><span class="function">IdleId <span class="title">registerIdle</span><span class="params">(<span class="keyword">int</span> idle, <span class="keyword">const</span> TcpConnPtr &amp;con, <span class="keyword">const</span> TcpCallBack &amp;cb)</span></span>;</span><br><span class="line"><span class="comment">//取消id对应的空闲连接监控事件</span></span><br><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">unregisterIdle</span><span class="params">(<span class="keyword">const</span> IdleId &amp;id)</span></span>;</span><br><span class="line"><span class="comment">//将id从所在的监控链表放到末尾</span></span><br><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">updateIdle</span><span class="params">(<span class="keyword">const</span> IdleId &amp;id)</span></span>;</span><br><span class="line"></span><br><span class="line"><span class="comment">//将超时的任务移除并执行，然后调用refreshNearest()</span></span><br><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">handleTimeouts</span><span class="params">()</span></span>;</span><br><span class="line"><span class="comment">//检查距离下一次超时的时间</span></span><br><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">refreshNearest</span><span class="params">(<span class="keyword">const</span> TimerId *tid = <span class="literal">NULL</span>)</span></span>;</span><br><span class="line"><span class="comment">//对于需要重复执行的超时任务，执行之前原子更新其定时器，回调函数为重新调用repeatableTimeout()</span></span><br><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">repeatableTimeout</span><span class="params">(TimerRepeatable *tr)</span></span>;</span><br><span class="line"><span class="comment">//添加定时任务</span></span><br><span class="line"><span class="comment">//对于循环定时任务TimerId.first = -milli，放进 timers_ 和 timerReps_ 里</span></span><br><span class="line"><span class="comment">//对于一次定时任务TimerId.first = milli，放进 timers_ 里</span></span><br><span class="line"><span class="function">TimerId <span class="title">runAt</span><span class="params">(<span class="keyword">int64_t</span> milli, <span class="built_in">Task</span> &amp;&amp;task, <span class="keyword">int64_t</span> interval)</span></span>;</span><br><span class="line"><span class="comment">//取消定时任务</span></span><br><span class="line"><span class="comment">//与runAt()规则一样，将其从 timers_ / timerReps_ 里删除</span></span><br><span class="line"><span class="function"><span class="keyword">bool</span> <span class="title">cancel</span><span class="params">(TimerId timerid)</span></span>;</span><br></pre></td></tr></table></figure>

<p><code>EventBase</code> 类只有一个事件分发器，只在主线程上运行，所有的事件处理也是在主线程上</p>
<p><code>MultiBase</code>类拥有多个事件分发器<code>EventBase</code> ，在执行<code>loop()</code>的时候，为第0 ~ n-1个事件分发器创建一个线程执行<code>EventBase::loop()</code>，第n个事件分发器（主Reactor，需处理accept）则在主线程上执行，所有的事件处理在各自的线程上</p>
<h2 id="conn-h"><a href="#conn-h" class="headerlink" title="conn.h"></a>conn.h</h2><p>分别使用<code>TcpConn</code>记录访问的tcp连接，<code>TcpServer</code>(包含事件分发器)处理具体的事件</p>
<blockquote>
<p><code>enable_shared_from_this&lt;&gt;</code> 和 <code>shared_from_this()</code></p>
<p>从<code>enable_shared_from_this</code>派生的对象可以使用成员函数中的 <code>shared_from_this</code> 方法创建一个 <code>shared_ptr</code> 所有者，与现有 <code>shared_ptr</code> 所有者共享实例所有权。<br>否则，如果通过 <code>this</code> 创建新的 <code>shared_ptr</code> ，则它与现有的 <code>shared_ptr</code> 所有者不同，这可能导致无效引用或导致该对象被删除多次。</p>
</blockquote>
<p>先来看看<code>TcpConn</code>类的一些成员函数：</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="comment">//用Channel封装套接字 并 设置回调函数</span></span><br><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">TcpConn::attach</span><span class="params">(EventBase *base, <span class="keyword">int</span> fd, Ip4Addr local, Ip4Addr peer)</span></span>;</span><br><span class="line"><span class="comment">//建立非阻塞socket的tcp连接</span></span><br><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">TcpConn::connect</span><span class="params">(EventBase *base, <span class="keyword">const</span> <span class="built_in">string</span> &amp;host, <span class="keyword">unsigned</span> short port, <span class="keyword">int</span> timeout, <span class="keyword">const</span> <span class="built_in">string</span> &amp;localip)</span></span>;</span><br><span class="line"><span class="comment">//将连接放到EventsImp的等待重连队列中，超时之后从队列中取出，重新connnect()</span></span><br><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">TcpConn::reconnect</span><span class="params">()</span></span>;</span><br><span class="line"><span class="comment">//设置重连时间间隔，-1: 不重连，0:立即重连，其它：等待毫秒数，未设置不重连</span></span><br><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">setReconnectInterval</span><span class="params">(<span class="keyword">int</span> milli)</span></span>;</span><br><span class="line"><span class="comment">//通过EventBase::safeCall()关闭channel_</span></span><br><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">TcpConn::close</span><span class="params">()</span></span>;</span><br><span class="line"></span><br><span class="line"><span class="comment">//表示当前连接失败或关闭</span></span><br><span class="line"><span class="comment">//(如果设置)重连 / 移出空闲监控 -&gt; 清除回调函数 -&gt; delete channel</span></span><br><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">TcpConn::cleanup</span><span class="params">(<span class="keyword">const</span> TcpConnPtr &amp;con)</span></span>;</span><br><span class="line"></span><br><span class="line"><span class="comment">//判断当前状态是否正确(connected)</span></span><br><span class="line"><span class="comment">//然后调用read()/write()</span></span><br><span class="line"><span class="comment">//根据返回的错误码执行相应的操作</span></span><br><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">TcpConn::handleRead</span><span class="params">(<span class="keyword">const</span> TcpConnPtr &amp;con)</span></span>;</span><br><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">TcpConn::handleWrite</span><span class="params">(<span class="keyword">const</span> TcpConnPtr &amp;con)</span></span>;</span><br><span class="line"></span><br><span class="line"><span class="comment">//当接收到消息且处于handshaking状态就需要调用该函数，使连接监听读/写-&gt;只监听读</span></span><br><span class="line">int TcpConn::handleHandshake(const TcpConnPtr &amp;con)；</span><br><span class="line"></span><br><span class="line"><span class="comment">//调用wirte()往socket写入数据，对不同的errno进行处理</span></span><br><span class="line"><span class="comment">//EINTR -&gt; 重新write()   </span></span><br><span class="line"><span class="comment">//EAGAIN or EWOULDBLOCK -&gt; 不继续发送，启用写事件监听，HandleWrite()处理</span></span><br><span class="line"><span class="function"><span class="keyword">ssize_t</span> <span class="title">TcpConn::isend</span><span class="params">(<span class="keyword">const</span> <span class="keyword">char</span> *buf, <span class="keyword">size_t</span> len)</span></span>;</span><br><span class="line"><span class="comment">//根据参数重载，实质调用isend()发送，如果未发送完成，添加到输出缓冲区，通过handleWrite()发送</span></span><br><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">TcpConn::send</span><span class="params">()</span></span>;</span><br><span class="line"></span><br><span class="line"><span class="comment">//两种模式: onRead()和onMsg()只能选择其一</span></span><br><span class="line"><span class="comment">//onMsg()也是通过调用onRead()实现，只是需要先decode</span></span><br><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">TcpConn::onMsg</span><span class="params">(CodecBase *codec, <span class="keyword">const</span> MsgCallBack &amp;cb)</span></span>;</span><br><span class="line"><span class="comment">//先encode再send()</span></span><br><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">TcpConn::sendMsg</span><span class="params">(Slice msg)</span></span>;</span><br><span class="line">    </span><br><span class="line"><span class="comment">//连接空闲idle秒后执行回调cb</span></span><br><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">TcpConn::addIdleCB</span><span class="params">(<span class="keyword">int</span> idle, <span class="keyword">const</span> TcpCallBack &amp;cb)</span></span>;</span><br></pre></td></tr></table></figure>

<blockquote>
<p>输入/输出缓冲区的设置：</p>
<p><a href="http://www.cppblog.com/Solstice/archive/2011/04/17/144378.html" target="_blank" rel="noopener">为什么 non-blocking 网络编程中应用层 buffer 是必须的？—— 陈硕的Blog</a></p>
</blockquote>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">struct</span> <span class="title">TcpServer</span> :</span> <span class="keyword">private</span> noncopyable &#123;</span><br><span class="line">    TcpServer(EventBases *bases);</span><br><span class="line">    <span class="comment">// return 0 on sucess, errno on error</span></span><br><span class="line">    <span class="comment">//创建socket,为socket设置SO_REUSEADDR/SO_REUSEPORT/FD_CLOEXEC</span></span><br><span class="line">    <span class="comment">//通过调用::bind绑定ip和端口</span></span><br><span class="line">    <span class="comment">//lsiten()设置该套接字等待处理的队列长度为20</span></span><br><span class="line">    <span class="comment">//将套接字封装为listen_channel_，设置套接字接收数据时的执行的函数</span></span><br><span class="line">    <span class="comment">//poller::loop_once()-&gt;Channel::handleRead()-&gt;TcpServer::handleAccept()</span></span><br><span class="line">    <span class="function"><span class="keyword">int</span> <span class="title">bind</span><span class="params">(<span class="keyword">const</span> <span class="built_in">std</span>::<span class="built_in">string</span> &amp;host, <span class="keyword">unsigned</span> short port, <span class="keyword">bool</span> reusePort = <span class="literal">false</span>)</span></span>;</span><br><span class="line">    <span class="comment">//创建以bases为事件分发器的tcpserver,调用TcpServer::bind()</span></span><br><span class="line">    <span class="function"><span class="keyword">static</span> TcpServerPtr <span class="title">startServer</span><span class="params">(EventBases *bases, <span class="keyword">const</span> <span class="built_in">std</span>::<span class="built_in">string</span> &amp;host, <span class="keyword">unsigned</span> short port, <span class="keyword">bool</span> reusePort = <span class="literal">false</span>)</span></span>;</span><br><span class="line">    ~TcpServer() &#123; <span class="keyword">delete</span> listen_channel_; &#125;</span><br><span class="line">    <span class="function">Ip4Addr <span class="title">getAddr</span><span class="params">()</span> </span>&#123; <span class="keyword">return</span> addr_; &#125;</span><br><span class="line">    <span class="function">EventBase *<span class="title">getBase</span><span class="params">()</span> </span>&#123; <span class="keyword">return</span> base_; &#125;</span><br><span class="line">    <span class="comment">//添加tcp连接时,为新的tcp连接所设置的回调函数</span></span><br><span class="line">    <span class="function"><span class="keyword">void</span> <span class="title">onConnCreate</span><span class="params">(<span class="keyword">const</span> <span class="built_in">std</span>::function&lt;TcpConnPtr()&gt; &amp;cb)</span> </span>&#123; createcb_ = cb; &#125;</span><br><span class="line">    <span class="function"><span class="keyword">void</span> <span class="title">onConnState</span><span class="params">(<span class="keyword">const</span> TcpCallBack &amp;cb)</span> </span>&#123; statecb_ = cb; &#125;</span><br><span class="line">    <span class="function"><span class="keyword">void</span> <span class="title">onConnRead</span><span class="params">(<span class="keyword">const</span> TcpCallBack &amp;cb)</span> </span>&#123;</span><br><span class="line">        readcb_ = cb;</span><br><span class="line">        assert(!msgcb_);</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="comment">// 消息处理与Read回调冲突，只能调用一个</span></span><br><span class="line">    <span class="function"><span class="keyword">void</span> <span class="title">onConnMsg</span><span class="params">(CodecBase *codec, <span class="keyword">const</span> MsgCallBack &amp;cb)</span> </span>&#123;</span><br><span class="line">        codec_.reset(codec);</span><br><span class="line">        msgcb_ = cb;</span><br><span class="line">        assert(!readcb_);</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">   <span class="keyword">private</span>:</span><br><span class="line">    EventBase *base_;<span class="comment">//当前服务器所使用的事件分发器</span></span><br><span class="line">    EventBases *bases_;<span class="comment">//负责为新连接分配事件分发器</span></span><br><span class="line">    Ip4Addr addr_;</span><br><span class="line">    Channel *listen_channel_;<span class="comment">//listen的套接字的Channel封装</span></span><br><span class="line">    <span class="comment">//创建的tcp新连接的回调函数</span></span><br><span class="line">    TcpCallBack statecb_, readcb_;</span><br><span class="line">    MsgCallBack msgcb_;</span><br><span class="line">    <span class="built_in">std</span>::function&lt;TcpConnPtr()&gt; createcb_;</span><br><span class="line">    <span class="built_in">std</span>::<span class="built_in">unique_ptr</span>&lt;CodecBase&gt; codec_;</span><br><span class="line">    <span class="comment">//循环调用accept()当前等待处理的队列</span></span><br><span class="line">    <span class="comment">//如果当前连接和分配给新连接的事件分发器一致，则直接创建新连接</span></span><br><span class="line">    <span class="comment">//如果不一致,则通过safeCall调用创建新连接</span></span><br><span class="line">    <span class="function"><span class="keyword">void</span> <span class="title">handleAccept</span><span class="params">()</span></span>;</span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure>

<p>还有一个半同步半异步服务器HSHA，利用线程池，为每一个消息处理都分配一个worker线程，即同步处理请求，异步管理IO</p>
<h2 id="udp-h"><a href="#udp-h" class="headerlink" title="udp.h"></a>udp.h</h2><p>参考conn.h</p>
<h2 id="http-h"><a href="#http-h" class="headerlink" title="http.h"></a>http.h</h2><p>HTTP消息是服务器和客户端之间交换数据的方式。有两种类型的消息：客户端发送的用于触发服务器上某个操作的<strong>请求</strong>和来自服务器的<strong>响应</strong>。</p>
<img src="/2020/04/18/handy/httpRequest.png" class="" title="HTTP-Request">

<img src="/2020/04/18/handy/httpResponse.png" class="" title="HTTP-Response">

<p>这里对这两种消息类型进行了抽象<code>HttpMsg</code>和实现<code>HttpRequest</code> / <code>HttpResponse</code></p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">struct</span> <span class="title">HttpMsg</span> &#123;</span></span><br><span class="line">    <span class="keyword">enum</span> Result &#123;Error, Complete, NotComplete, Continue100,&#125;;</span><br><span class="line">    HttpMsg() &#123; HttpMsg::<span class="built_in">clear</span>(); &#125;;</span><br><span class="line">    <span class="comment">//内容添加到buf，返回写入的字节数</span></span><br><span class="line">    <span class="function"><span class="keyword">virtual</span> <span class="keyword">int</span> <span class="title">encode</span><span class="params">(Buffer &amp;buf)</span> </span>= <span class="number">0</span>;</span><br><span class="line">    <span class="comment">//尝试从buf中解析，默认复制body内容</span></span><br><span class="line">    <span class="function"><span class="keyword">virtual</span> Result <span class="title">tryDecode</span><span class="params">(Slice buf, <span class="keyword">bool</span> copyBody = <span class="literal">true</span>)</span> </span>= <span class="number">0</span>;</span><br><span class="line">    <span class="comment">//清空消息相关的字段</span></span><br><span class="line">    <span class="function"><span class="keyword">virtual</span> <span class="keyword">void</span> <span class="title">clear</span><span class="params">()</span></span>;</span><br><span class="line">	</span><br><span class="line">    <span class="function"><span class="built_in">std</span>::<span class="built_in">string</span> <span class="title">getHeader</span><span class="params">(<span class="keyword">const</span> <span class="built_in">std</span>::<span class="built_in">string</span> &amp;n)</span> </span>&#123; <span class="keyword">return</span> getValueFromMap_(headers, n); &#125;</span><br><span class="line">    <span class="function">Slice <span class="title">getBody</span><span class="params">()</span> </span>&#123; <span class="keyword">return</span> body2.<span class="built_in">size</span>() ? body2 : (Slice) body; &#125;</span><br><span class="line"></span><br><span class="line">    <span class="comment">//如果tryDecode返回Complete，则返回已解析的字节数</span></span><br><span class="line">    <span class="function"><span class="keyword">int</span> <span class="title">getByte</span><span class="params">()</span> </span>&#123; <span class="keyword">return</span> scanned_; &#125;</span><br><span class="line">	</span><br><span class="line">    <span class="comment">//保存Status line/Headers/Body</span></span><br><span class="line">    <span class="built_in">std</span>::<span class="built_in">map</span>&lt;<span class="built_in">std</span>::<span class="built_in">string</span>, <span class="built_in">std</span>::<span class="built_in">string</span>&gt; headers;</span><br><span class="line">    <span class="built_in">std</span>::<span class="built_in">string</span> version, body;</span><br><span class="line">    <span class="comment">// body可能较大，为了避免数据复制，加入body2</span></span><br><span class="line">    Slice body2;</span><br><span class="line">    </span><br><span class="line">   <span class="keyword">protected</span>:</span><br><span class="line">    <span class="keyword">bool</span> complete_;<span class="comment">//是否完整读完一个消息</span></span><br><span class="line">    <span class="keyword">size_t</span> contentLen_;<span class="comment">//消息主体长度</span></span><br><span class="line">    <span class="keyword">size_t</span> scanned_;<span class="comment">//已经读过的长度</span></span><br><span class="line">    <span class="comment">//line1存储第一行</span></span><br><span class="line">    <span class="comment">//以第一个\r\n\r\n作为header和body的分割，如未找到，则header未读完，返回NotComplete</span></span><br><span class="line">    <span class="comment">//循环读入每一行的，以第一个词(保证跟着":")为key, 后面为value保存到headers中</span></span><br><span class="line">    <span class="comment">//最后根据headers中的"content-length"判断是否读完整个Http Message，没有则返回Continue100</span></span><br><span class="line">    <span class="comment">//如果选择copyBody,则将消息主体拷贝到body中，否则仅仅将指针存放在body2中，返回Complete </span></span><br><span class="line">    <span class="function">Result <span class="title">tryDecode_</span><span class="params">(Slice buf, <span class="keyword">bool</span> copyBody, Slice *line1)</span></span>;</span><br><span class="line">    <span class="function"><span class="built_in">std</span>::<span class="built_in">string</span> <span class="title">getValueFromMap_</span><span class="params">(<span class="built_in">std</span>::<span class="built_in">map</span>&lt;<span class="built_in">std</span>::<span class="built_in">string</span>, <span class="built_in">std</span>::<span class="built_in">string</span>&gt; &amp;m, <span class="keyword">const</span> <span class="built_in">std</span>::<span class="built_in">string</span> &amp;n)</span></span>;</span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure>

<blockquote>
<p>HTTP Request Target <a href="https://tools.ietf.org/html/rfc7230#page-41" target="_blank" rel="noopener">Request Target</a> / <a href="https://developer.mozilla.org/en-US/docs/Web/HTTP/Messages" target="_blank" rel="noopener">HTTP Message - MDN web docs</a></p>
<ul>
<li><p>origin-form</p>
<p>absolute-path [ “?” query ]</p>
<p>the most common form, and it is used with <code>GET</code>, <code>POST</code>, <code>HEAD</code>, and <code>OPTIONS</code> methods.</p>
<p>e.g. GET /background.png HTTP/1.0    HEAD /test.html?query=alibaba HTTP/1.1</p>
</li>
<li><p>absolute-form</p>
<p>absolute-URI</p>
<p>e.g. GET <a href="http://developer.mozilla.org/en-US/docs/Web/HTTP/Messages" target="_blank" rel="noopener">http://developer.mozilla.org/en-US/docs/Web/HTTP/Messages</a> HTTP/1.1</p>
</li>
<li><p>authority-form</p>
<p>The authority component of a URL, consisting of the domain name and optionally the port (prefixed by a ‘:’), is called the authority form. It is only used with CONNECT when setting up an HTTP tunnel.</p>
<p>e.g. CONNECT developer.mozilla.org:80 HTTP/1.1</p>
</li>
<li><p>asterisk-form</p>
<p>Used with <code>OPTIONS</code>, representing the server as a whole.</p>
<p>e.g. OPTIONS * HTTP/1.1</p>
</li>
</ul>
</blockquote>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">struct</span> <span class="title">HttpRequest</span> :</span> <span class="keyword">public</span> HttpMsg &#123;</span><br><span class="line">    HttpRequest() &#123; <span class="built_in">clear</span>(); &#125;</span><br><span class="line">    <span class="built_in">std</span>::<span class="built_in">map</span>&lt;<span class="built_in">std</span>::<span class="built_in">string</span>, <span class="built_in">std</span>::<span class="built_in">string</span>&gt; args;</span><br><span class="line">    <span class="comment">//保存HTTP method(GET/PUT/POST/HEAD...)</span></span><br><span class="line">    <span class="comment">//query_uri保存完整的request target</span></span><br><span class="line">    <span class="comment">//而uri则保存其中的绝对路径,在请求中与查询字符串以?分隔</span></span><br><span class="line">    <span class="built_in">std</span>::<span class="built_in">string</span> method, uri, query_uri;</span><br><span class="line">    <span class="function"><span class="built_in">std</span>::<span class="built_in">string</span> <span class="title">getArg</span><span class="params">(<span class="keyword">const</span> <span class="built_in">std</span>::<span class="built_in">string</span> &amp;n)</span> </span>&#123; <span class="keyword">return</span> getValueFromMap_(args, n); &#125;</span><br><span class="line"></span><br><span class="line">    <span class="comment">// override</span></span><br><span class="line">    <span class="comment">// 按格式将Start line/headers(手动添加content-length和长连接)/body添加到buf中</span></span><br><span class="line">    <span class="function"><span class="keyword">virtual</span> <span class="keyword">int</span> <span class="title">encode</span><span class="params">(Buffer &amp;buf)</span></span>;</span><br><span class="line">    <span class="comment">//只支持origin-form的解析</span></span><br><span class="line">    <span class="comment">//将query_uri中"?"后面的查询字符串，通过查找"&amp;"和"=",提取query参数到args中</span></span><br><span class="line">    <span class="function"><span class="keyword">virtual</span> Result <span class="title">tryDecode</span><span class="params">(Slice buf, <span class="keyword">bool</span> copyBody = <span class="literal">true</span>)</span></span>;</span><br><span class="line">    <span class="function"><span class="keyword">virtual</span> <span class="keyword">void</span> <span class="title">clear</span><span class="params">()</span> </span>&#123;</span><br><span class="line">        HttpMsg::<span class="built_in">clear</span>();</span><br><span class="line">        args.<span class="built_in">clear</span>();</span><br><span class="line">        method = <span class="string">"GET"</span>;</span><br><span class="line">        query_uri = uri = <span class="string">""</span>;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure>

<p><code>HttpResponse</code>类和<code>HttpRequest</code>只有以下区别（不考虑HTTP1.1和2的区别(chunked)）：</p>
<ul>
<li><p>HTTP response的起始行叫做<code>status line</code>，包含协议版本/状态码/状态的简单描述</p>
<p>e.g. HTTP/1.1 404 Not Found</p>
</li>
<li><p>与Request Header相对应的是Response Header，提供有关服务器的其他信息</p>
<p>e.g. Vary / Accept-Ranges </p>
</li>
</ul>
<p>所以解码只需通过<code>HttpMsg</code>继承的<code>tryDecode_(Slice buf, bool copyBody, Slice *line1)</code>完成，然后单独处理<code>line1</code></p>
<p><code>HttpConnPtr</code>类是本质上是一条Tcp连接，进一步封装主要是加入了HttpRequest，HttpResponse的处理</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">struct</span> <span class="title">HttpConnPtr</span> &#123;</span></span><br><span class="line">    TcpConnPtr tcp;</span><br><span class="line">    HttpConnPtr(<span class="keyword">const</span> TcpConnPtr &amp;con) : tcp(con) &#123;&#125;</span><br><span class="line">    <span class="comment">//允许将HttpConnPtr转换成TcpConnPtr,就是获得其中的tcp</span></span><br><span class="line">    <span class="function"><span class="keyword">operator</span> <span class="title">TcpConnPtr</span><span class="params">()</span> <span class="keyword">const</span> </span>&#123; <span class="keyword">return</span> tcp; &#125;</span><br><span class="line">    <span class="comment">//重载-&gt;，返回shared_ptr&lt;TcpCoon&gt;中存储的指针</span></span><br><span class="line">    TcpConn *<span class="keyword">operator</span>-&gt;() <span class="keyword">const</span> &#123; <span class="keyword">return</span> tcp.<span class="built_in">get</span>(); &#125;</span><br><span class="line">    <span class="keyword">bool</span> <span class="keyword">operator</span>&lt;(<span class="keyword">const</span> HttpConnPtr &amp;con) <span class="keyword">const</span> &#123; <span class="keyword">return</span> tcp &lt; con.tcp; &#125;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">typedef</span> <span class="built_in">std</span>::function&lt;<span class="keyword">void</span>(<span class="keyword">const</span> HttpConnPtr &amp;)&gt; HttpCallBack;</span><br><span class="line">	<span class="comment">//利用AutoContext类实现HttpContext的内存管理 并 提供访问</span></span><br><span class="line">    <span class="function">HttpRequest &amp;<span class="title">getRequest</span><span class="params">()</span> <span class="keyword">const</span> </span>&#123; <span class="keyword">return</span> tcp-&gt;internalCtx_.context&lt;HttpContext&gt;().req; &#125;</span><br><span class="line">    <span class="function">HttpResponse &amp;<span class="title">getResponse</span><span class="params">()</span> <span class="keyword">const</span> </span>&#123; <span class="keyword">return</span> tcp-&gt;internalCtx_.context&lt;HttpContext&gt;().resp; &#125;</span><br><span class="line">    <span class="function"><span class="keyword">void</span> <span class="title">sendRequest</span><span class="params">()</span> <span class="keyword">const</span> </span>&#123; sendRequest(getRequest()); &#125;</span><br><span class="line">    <span class="function"><span class="keyword">void</span> <span class="title">sendResponse</span><span class="params">()</span> <span class="keyword">const</span> </span>&#123; sendResponse(getResponse()); &#125;</span><br><span class="line">    <span class="comment">//将http request编码后放到tcp的output缓冲区进行发送</span></span><br><span class="line">    <span class="function"><span class="keyword">void</span> <span class="title">sendRequest</span><span class="params">(HttpRequest &amp;req)</span> <span class="keyword">const</span> </span>&#123;&#125;</span><br><span class="line">    <span class="comment">//将http response编码后放到tcp的output缓冲区进行发送</span></span><br><span class="line">    <span class="function"><span class="keyword">void</span> <span class="title">sendResponse</span><span class="params">(HttpResponse &amp;resp)</span> <span class="keyword">const</span> </span>&#123;&#125;</span><br><span class="line">    <span class="comment">//文件作为Response</span></span><br><span class="line">    <span class="comment">//先读取文件内容，然后将内容指针存到getResponse()的body2,sendResponse()</span></span><br><span class="line">    <span class="function"><span class="keyword">void</span> <span class="title">sendFile</span><span class="params">(<span class="keyword">const</span> <span class="built_in">std</span>::<span class="built_in">string</span> &amp;filename)</span> <span class="keyword">const</span></span>;</span><br><span class="line">    <span class="comment">//客户端:将tcp的输入缓冲区中已解析为HttpResponse的数据移除,然后清空getResponse()</span></span><br><span class="line">    <span class="comment">//服务端:将tcp的输入缓冲区中已解析为HttpRequest的数据移除,然后清空getRequest()</span></span><br><span class="line">    <span class="function"><span class="keyword">void</span> <span class="title">clearData</span><span class="params">()</span> <span class="keyword">const</span></span>;</span><br><span class="line">	<span class="comment">//设置tcp数据到达时的回调函数:</span></span><br><span class="line">    <span class="comment">//为tcp生成一个HttpConnPtr对象从而使用其中的handleRead()</span></span><br><span class="line">    <span class="comment">//将tcp缓冲区中解析到getRequest()/getResponse(),并执行回调函数cb()</span></span><br><span class="line">    <span class="function"><span class="keyword">void</span> <span class="title">onHttpMsg</span><span class="params">(<span class="keyword">const</span> HttpCallBack &amp;cb)</span> <span class="keyword">const</span></span>;</span><br><span class="line">    <span class="comment">//当数据到达</span></span><br><span class="line">    <span class="comment">//poller::loop_once()-&gt;Channel::handleRead()-&gt;TcpConn::handleRead()</span></span><br><span class="line">    <span class="comment">//-&gt;TcpConn::readcb_()</span></span><br><span class="line"></span><br><span class="line">   <span class="keyword">protected</span>:</span><br><span class="line">    <span class="class"><span class="keyword">struct</span> <span class="title">HttpContext</span> &#123;</span></span><br><span class="line">        HttpRequest req;</span><br><span class="line">        HttpResponse resp;</span><br><span class="line">    &#125;;</span><br><span class="line">    <span class="comment">//如果是服务器,从tcp的输入缓冲区解析到getRequest()</span></span><br><span class="line">    <span class="comment">//解析出错，则关闭tcp连接,解析未完成则请求客户端继续发送,解析完成执行回调函数</span></span><br><span class="line">    <span class="comment">//如果是客户端,从tcp的输入缓冲区解析到getResponse()</span></span><br><span class="line">    <span class="comment">//解析出错，则关闭tcp连接,解析完成执行回调函数</span></span><br><span class="line">    <span class="function"><span class="keyword">void</span> <span class="title">handleRead</span><span class="params">(<span class="keyword">const</span> HttpCallBack &amp;cb)</span> <span class="keyword">const</span></span>;</span><br><span class="line">    <span class="function"><span class="keyword">void</span> <span class="title">logOutput</span><span class="params">(<span class="keyword">const</span> <span class="keyword">char</span> *title)</span> <span class="keyword">const</span></span>;</span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure>

<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">struct</span> <span class="title">HttpServer</span> :</span> <span class="keyword">public</span> TcpServer &#123;</span><br><span class="line">    <span class="comment">//TcpServer(bases)</span></span><br><span class="line">    <span class="comment">//设置当前tcp连接accept其他connect请求时,新的tcp的连接方式:</span></span><br><span class="line">    <span class="comment">//创建一条新的tcp连接,用它生成一个HttpConnPtr,设置该HttpConnPtr的onHttpMsg():</span></span><br><span class="line">    <span class="comment">//当HttpConnPtr所拥有的tcp数据到来时,解析为httpRequest,并执行对应的cbs_,再执行defcb_</span></span><br><span class="line">    HttpServer(EventBases *bases);</span><br><span class="line">    <span class="keyword">template</span> &lt;<span class="class"><span class="keyword">class</span> <span class="title">Conn</span> = <span class="title">TcpConn</span>&gt;</span></span><br><span class="line"><span class="class">    <span class="title">void</span> <span class="title">setConnType</span>() &#123;</span></span><br><span class="line">        conncb_ = [] &#123; <span class="keyword">return</span> TcpConnPtr(<span class="keyword">new</span> Conn); &#125;;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="comment">//设置当接收到GET uri的请求时,需要执行的函数</span></span><br><span class="line">    <span class="function"><span class="keyword">void</span> <span class="title">onGet</span><span class="params">(<span class="keyword">const</span> <span class="built_in">std</span>::<span class="built_in">string</span> &amp;uri, <span class="keyword">const</span> HttpCallBack &amp;cb)</span> </span>&#123; cbs_[<span class="string">"GET"</span>][uri] = cb; &#125;</span><br><span class="line">    <span class="comment">//设置当接收到何种方法,何种uri的请求时需要执行的函数</span></span><br><span class="line">    <span class="function"><span class="keyword">void</span> <span class="title">onRequest</span><span class="params">(<span class="keyword">const</span> <span class="built_in">std</span>::<span class="built_in">string</span> &amp;method, <span class="keyword">const</span> <span class="built_in">std</span>::<span class="built_in">string</span> &amp;uri, <span class="keyword">const</span> HttpCallBack &amp;cb)</span> </span>&#123; cbs_[method][uri] = cb; &#125;</span><br><span class="line">    <span class="comment">//设置接收并以对应的方式处理完之后还需要执行的函数</span></span><br><span class="line">    <span class="comment">//默认是发送404 Not Found的Http Response</span></span><br><span class="line">    <span class="function"><span class="keyword">void</span> <span class="title">onDefault</span><span class="params">(<span class="keyword">const</span> HttpCallBack &amp;cb)</span> </span>&#123; defcb_ = cb; &#125;</span><br><span class="line">   <span class="keyword">private</span>:</span><br><span class="line">    HttpCallBack defcb_;	<span class="comment">//处理完请求之后的回调函数</span></span><br><span class="line">    <span class="built_in">std</span>::function&lt;TcpConnPtr()&gt; conncb_;</span><br><span class="line">    <span class="built_in">std</span>::<span class="built_in">map</span>&lt;<span class="built_in">std</span>::<span class="built_in">string</span>, <span class="built_in">std</span>::<span class="built_in">map</span>&lt;<span class="built_in">std</span>::<span class="built_in">string</span>, HttpCallBack&gt;&gt; cbs_;</span><br><span class="line">    <span class="comment">//保存[method][uri]对应请求所需执行的函数</span></span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure>

<h2 id="conf-h"><a href="#conf-h" class="headerlink" title="conf.h"></a>conf.h</h2><p><code>Conf</code>类是对INI配置文件读写的封装</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="comment">// 0 success</span></span><br><span class="line"><span class="comment">// -1 IOERROR</span></span><br><span class="line"><span class="comment">// &gt;0 line no of error</span></span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">parse</span><span class="params">(<span class="keyword">const</span> <span class="built_in">std</span>::<span class="built_in">string</span> &amp;filename)</span></span>;</span><br><span class="line"></span><br><span class="line"><span class="comment">// Get a string value from INI file</span></span><br><span class="line"><span class="function"><span class="built_in">std</span>::<span class="built_in">string</span> <span class="title">get</span><span class="params">(<span class="built_in">std</span>::<span class="built_in">string</span> section, <span class="built_in">std</span>::<span class="built_in">string</span> name, <span class="built_in">std</span>::<span class="built_in">string</span> default_value)</span></span>;</span><br><span class="line"><span class="comment">// Get an integer (long) value from INI file</span></span><br><span class="line"><span class="function"><span class="keyword">long</span> <span class="title">getInteger</span><span class="params">(<span class="built_in">std</span>::<span class="built_in">string</span> section, <span class="built_in">std</span>::<span class="built_in">string</span> name, <span class="keyword">long</span> default_value)</span></span>;</span><br><span class="line"><span class="comment">// Get a real (floating point double) value from INI file</span></span><br><span class="line"><span class="function"><span class="keyword">double</span> <span class="title">getReal</span><span class="params">(<span class="built_in">std</span>::<span class="built_in">string</span> section, <span class="built_in">std</span>::<span class="built_in">string</span> name, <span class="keyword">double</span> default_value)</span></span>;</span><br><span class="line"><span class="comment">// Get a boolean value from INI file</span></span><br><span class="line"><span class="function"><span class="keyword">bool</span> <span class="title">getBoolean</span><span class="params">(<span class="built_in">std</span>::<span class="built_in">string</span> section, <span class="built_in">std</span>::<span class="built_in">string</span> name, <span class="keyword">bool</span> default_value)</span></span>;</span><br><span class="line"><span class="comment">// Get a string value from INI file</span></span><br><span class="line"><span class="function"><span class="built_in">std</span>::<span class="built_in">list</span>&lt;<span class="built_in">std</span>::<span class="built_in">string</span>&gt; <span class="title">getStrings</span><span class="params">(<span class="built_in">std</span>::<span class="built_in">string</span> section, <span class="built_in">std</span>::<span class="built_in">string</span> name)</span></span>;</span><br><span class="line"></span><br><span class="line"><span class="built_in">std</span>::<span class="built_in">map</span>&lt;<span class="built_in">std</span>::<span class="built_in">string</span>, <span class="built_in">std</span>::<span class="built_in">list</span>&lt;<span class="built_in">std</span>::<span class="built_in">string</span>&gt;&gt; values_;</span><br><span class="line"><span class="built_in">std</span>::<span class="built_in">string</span> filename;</span><br></pre></td></tr></table></figure>

<h2 id="daemon-h"><a href="#daemon-h" class="headerlink" title="daemon.h"></a>daemon.h</h2><blockquote>
<p>参考<a href="http://www.enderunix.org/docs/eng/daemon.php" target="_blank" rel="noopener">Unix Daemon Server Programming</a></p>
</blockquote>
<p>创建守护进程一般有以下几步</p>
<ol>
<li><p><code>fork()</code>父进程，让父进程退出，则孤儿进程会成为 <code>init</code> 的子进程</p>
</li>
<li><p>调用<code>setsid()</code>创建新会话，当前进程成为 新会话&amp;新进程组 的领导进程，没有控制tty</p>
<blockquote>
<p>在Unix系统中，进程在进程组内运行，因此在一个进程组中的所有进程被视为单个实体，子进程也会继承父进程的进程组和会话。而服务器不应从启动它的进程中接收信号，因此它必须将自己与控制tty分离，所以我们需要通过<code>setsid()</code>进一步独立进程</p>
</blockquote>
</li>
<li><p>大多数服务器以超级用户身份运行，出于安全原因，它们应保护自己创建的文件。<code>umask()</code> 设置用户掩码将防止在创建文件时可能出现的不安全文件特权。</p>
</li>
<li><p>服务器应在已知目录中运行 <code>chdir(&quot;/servers/&quot;)</code></p>
</li>
<li><p>大多数服务一次只需要运行一个服务器副本。因此我们通过文件锁定<code>loackf</code>来保持单例。服务器的第一个实例将锁定文件，以便其他实例了解实例已在运行。 如果服务器终止，锁将自动释放，以便新实例可以运行。 </p>
</li>
<li><p>进程可能会从用户或进程接收信号，因此最好抓住这些信号并相应地执行操作。 子进程终止时会发送SIGCHLD信号，服务器进程必须忽略或处理这些信号。</p>
</li>
<li><p>日志消息的处理 -&gt; 标准IO / 写入文件 / 系统日志守护进程syslogd</p>
</li>
</ol>
<h2 id="stat-svr-h"><a href="#stat-svr-h" class="headerlink" title="stat-svr.h"></a>stat-svr.h</h2><p>对<code>HttpServer</code>添加query 参数stat，方便外部的工具查看应用程序的状态</p>
<h2 id="总结"><a href="#总结" class="headerlink" title="总结"></a>总结</h2><p>​    <code>EventBases</code>负责线程与<code>EventBase</code>（持有<code>poller</code>）之间的联系，底层的<code>poller</code>（使用epoll水平触发） 检测到<code>POLLIN</code> / <code>POLLOUT</code>读写事件时，调用事件对应 <code>Channel</code> 的 <code>handleRead()</code> / <code>handleWrite()</code> 执行 I/O，具体的操作取决于上层的协议，如<code>TcpCoon</code> 所持有的套接字的Channel的就会调用<code>TcpConn::handleRead()</code>，然后一次性把 socket 里的数据读完（从操作系统 buffer 搬到应用层 buffer），否则会反复触发 <code>POLLIN</code> 事件，造成 <code>busy-loop</code> 。对于数据不完整的情况，则数据依然存放在应用层buffer中直到构成完整的消息，因此每个 <code>TcpConn</code> 都需配置 <code>input buffer</code>，同理也需要<code>output buffer</code>，并且程序只负责调用<code>send</code>往应用层buffer中添加发送内容，然后由库接手，为 <code>socket</code> 注册 <code>POLLOUT</code> 事件，只要<code>socket</code>可写就将数据从应用层buffer搬到操作系统buffer，直到写完后停止<code>POLLOUT</code></p>
<img src="/2020/04/18/handy/reactor.png" class="" title="Reactor">

<p>线程模型：</p>
<p>每个线程最多有一个 <code>EventBase</code>，每个<code>TcpConn</code> 所在的线程由其所属的  <code>EventBase</code> （<code>TcpServer</code>通过调用<code>allocBase()</code>分配）决定</p>
<p>所有对IO和buffer的读写，都应该在IO线程中完成</p>
<p>单线程：同步处理请求，同步管理IO，全部在主线程完成，不需要考虑线程安全</p>
<p>多线程：</p>
<ul>
<li><p>MultiBase —— 通过vector + atomic&lt;int&gt;实现对 <code>EventBase</code> 的分配（Round Robin）</p>
<p>（类似于多个单线程模型，只是需要考虑线程安全）</p>
<p>一个reactor对应一个<code>EventBase</code>。主Reactor只有一个，只负责监听新的连接，accept后将这个连接分配到子Reactor上，子Reactor可以有多个，这样可以分摊一个<code>EventBase</code>的压力。</p>
<img src="/2020/04/18/handy/reactor-master.png" class="" title="Reactor-master">
</li>
<li><p>HSHA服务器（同步处理请求，异步管理IO）则主要是实现了上图下部的<code>Thread Pool</code>的例子，可以设置消息模式时，将每一个读写任务添加到queued tasks中即业务线程池，适用于阻塞型或者耗时型的任务，尽可能不影响每一个<code>EventBase::loop()</code> </p>
</li>
</ul>
]]></content>
      <categories>
        <category>Programing</category>
      </categories>
      <tags>
        <tag>C/C++</tag>
        <tag>Linux</tag>
        <tag>Socket</tag>
      </tags>
  </entry>
</search>
